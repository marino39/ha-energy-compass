[project]
name = "ha-energy-compass"
version = "0.1.18"
requires-python = ">=3.14.2"
dependencies = [
    "scipy==1.18.1",
]

[project.optional-dependencies]
test = [
    "pytest-homeassistant-custom-component==0.13.364",
    "ruff==0.16.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
