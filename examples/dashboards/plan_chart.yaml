# Generated by tools/dashboards/build.py - do not edit; edit the generator and rebuild.
type: grid
column_span: 4
cards:
- type: heading
  heading: Energy Compass
  heading_style: title
  icon: mdi:chart-timeline-variant
- type: custom:apexcharts-card
  section_mode: true
  grid_options:
    columns: full
    rows: 9
  header:
    show: true
    title: Load / Grid / PV / SoC - history and plan
    show_states: true
    colorize_states: true
  graph_span: 36h
  span:
    offset: +24h
  update_interval: 1min
  hours_12: false
  now:
    show: true
    label: Now
    color: '#9e9e9e'
  show:
    last_updated: true
  yaxis:
  - id: power
    decimals: 1
    min: ~0
    max: ~0
    apex_config:
      tickAmount: 6
      title:
        text: Power [kW]
  - id: soc
    opposite: true
    min: 0
    max: 100
    decimals: 0
    apex_config:
      tickAmount: 5
      title:
        text: SoC [%]
  apex_config:
    chart:
      animations:
        enabled: false
      toolbar:
        show: false
      events:
        mounted: "EVAL:function (chart) {\n  // ApexCharts Card 2.2.3 keeps the latest HA state on its shadow-root host.\n\
          \  const hass = chart.el.getRootNode().host?._hass;\n  for (const id of chart.__energyCompassAnnotationIds || [])\
          \ chart.removeAnnotation(id);\n  chart.__energyCompassAnnotationIds = [];\n  const plan = hass?.states['sensor.replace_with_plan'];\n\
          \  const valid = hass?.states['binary_sensor.replace_with_forecast_valid'];\n  const now = Date.now();\n  if (!plan\
          \ || ['unknown', 'unavailable'].includes(plan.state)) return;\n  if (plan.attributes.refreshing !== true &&\n  \
          \    (valid?.state !== 'on' || !(Date.parse(plan.attributes.valid_until) > now))) return;\n\n  const min = Math.max(now,\
          \ chart.w.globals.minX);\n  const max = chart.w.globals.maxX;\n  if (!Number.isFinite(min) || !Number.isFinite(max)\
          \ || max <= min) return;\n  const stateColors = {\n    CHARGE_PV: '#fdd835',\n    CHARGE_GRID: '#ef5350',\n    DISCHARGE_GRID:\
          \ '#ab47bc',\n    SELF_CONSUME: '#66bb6a',\n  };\n  const intervals = (plan.attributes.intervals || []).map(row\
          \ => ({\n    start: Math.max(min, Date.parse(row.start)), end: Math.min(max, Date.parse(row.end)),\n    state: row.state,\n\
          \  })).filter(row => Number.isFinite(row.start) && row.end > row.start)\n    .sort((a, b) => a.start - b.start);\n\
          \  const runs = [];\n  for (const interval of intervals) {\n    const lastRun = runs[runs.length - 1];\n    if (lastRun\
          \ && lastRun.state === interval.state && lastRun.end === interval.start) {\n      lastRun.end = interval.end;\n\
          \    } else runs.push({...interval});\n  }\n\n  const add = (annotation) => {\n    chart.addXaxisAnnotation(annotation,\
          \ false);\n    chart.__energyCompassAnnotationIds.push(annotation.id);\n  };\n  runs.forEach((band, index) => add({\n\
          \    id: 'ec-background-' + index,\n    x: band.start, x2: band.end,\n    fillColor: stateColors[band.state] ||\
          \ '#78909c', opacity: 0.14,\n    borderColor: 'transparent', strokeDashArray: 0,\n  }));\n\n  runs.forEach((run,\
          \ index) => {\n    const width = (run.end - run.start) / (max - chart.w.globals.minX) * chart.w.globals.gridWidth;\n\
          \    const label = width >= 13 ? {\n      text: run.state, position: 'top', orientation: 'vertical', offsetY: 8,\
          \ offsetX: width / 2,\n      borderWidth: 0,\n      style: {background: 'transparent', color: hass?.themes?.darkMode\
          \ === false ? '#616161' : '#bdbdbd', fontSize: '9px', fontWeight: 700,\n        padding: {left: 1, right: 1, top:\
          \ 1, bottom: 1}},\n    } : {text: ''};\n    add({id: 'ec-state-' + index, x: run.start, x2: run.end,\n      fillColor:\
          \ 'transparent', opacity: 0, borderColor: '#616161', strokeDashArray: 3, label});\n  });\n\n  // Native SVG titles\
          \ expose even intervals too narrow for a visible label.\n  for (const [index, run] of runs.entries()) {\n    const\
          \ time = stamp => new Date(stamp).toLocaleTimeString('en-GB', {hour: '2-digit', minute: '2-digit'});\n    const\
          \ elements = chart.el.querySelectorAll('.ec-state-' + index);\n    for (const element of elements) {\n      const\
          \ title = document.createElementNS('http://www.w3.org/2000/svg', 'title');\n      title.textContent = `${run.state}\
          \ · ${time(run.start)}–${time(run.end)}`;\n      element.appendChild(title);\n    }\n  }\n}\n"
        updated: "EVAL:function (chart) {\n  // ApexCharts Card 2.2.3 keeps the latest HA state on its shadow-root host.\n\
          \  const hass = chart.el.getRootNode().host?._hass;\n  for (const id of chart.__energyCompassAnnotationIds || [])\
          \ chart.removeAnnotation(id);\n  chart.__energyCompassAnnotationIds = [];\n  const plan = hass?.states['sensor.replace_with_plan'];\n\
          \  const valid = hass?.states['binary_sensor.replace_with_forecast_valid'];\n  const now = Date.now();\n  if (!plan\
          \ || ['unknown', 'unavailable'].includes(plan.state)) return;\n  if (plan.attributes.refreshing !== true &&\n  \
          \    (valid?.state !== 'on' || !(Date.parse(plan.attributes.valid_until) > now))) return;\n\n  const min = Math.max(now,\
          \ chart.w.globals.minX);\n  const max = chart.w.globals.maxX;\n  if (!Number.isFinite(min) || !Number.isFinite(max)\
          \ || max <= min) return;\n  const stateColors = {\n    CHARGE_PV: '#fdd835',\n    CHARGE_GRID: '#ef5350',\n    DISCHARGE_GRID:\
          \ '#ab47bc',\n    SELF_CONSUME: '#66bb6a',\n  };\n  const intervals = (plan.attributes.intervals || []).map(row\
          \ => ({\n    start: Math.max(min, Date.parse(row.start)), end: Math.min(max, Date.parse(row.end)),\n    state: row.state,\n\
          \  })).filter(row => Number.isFinite(row.start) && row.end > row.start)\n    .sort((a, b) => a.start - b.start);\n\
          \  const runs = [];\n  for (const interval of intervals) {\n    const lastRun = runs[runs.length - 1];\n    if (lastRun\
          \ && lastRun.state === interval.state && lastRun.end === interval.start) {\n      lastRun.end = interval.end;\n\
          \    } else runs.push({...interval});\n  }\n\n  const add = (annotation) => {\n    chart.addXaxisAnnotation(annotation,\
          \ false);\n    chart.__energyCompassAnnotationIds.push(annotation.id);\n  };\n  runs.forEach((band, index) => add({\n\
          \    id: 'ec-background-' + index,\n    x: band.start, x2: band.end,\n    fillColor: stateColors[band.state] ||\
          \ '#78909c', opacity: 0.14,\n    borderColor: 'transparent', strokeDashArray: 0,\n  }));\n\n  runs.forEach((run,\
          \ index) => {\n    const width = (run.end - run.start) / (max - chart.w.globals.minX) * chart.w.globals.gridWidth;\n\
          \    const label = width >= 13 ? {\n      text: run.state, position: 'top', orientation: 'vertical', offsetY: 8,\
          \ offsetX: width / 2,\n      borderWidth: 0,\n      style: {background: 'transparent', color: hass?.themes?.darkMode\
          \ === false ? '#616161' : '#bdbdbd', fontSize: '9px', fontWeight: 700,\n        padding: {left: 1, right: 1, top:\
          \ 1, bottom: 1}},\n    } : {text: ''};\n    add({id: 'ec-state-' + index, x: run.start, x2: run.end,\n      fillColor:\
          \ 'transparent', opacity: 0, borderColor: '#616161', strokeDashArray: 3, label});\n  });\n\n  // Native SVG titles\
          \ expose even intervals too narrow for a visible label.\n  for (const [index, run] of runs.entries()) {\n    const\
          \ time = stamp => new Date(stamp).toLocaleTimeString('en-GB', {hour: '2-digit', minute: '2-digit'});\n    const\
          \ elements = chart.el.querySelectorAll('.ec-state-' + index);\n    for (const element of elements) {\n      const\
          \ title = document.createElementNS('http://www.w3.org/2000/svg', 'title');\n      title.textContent = `${run.state}\
          \ · ${time(run.start)}–${time(run.end)}`;\n      element.appendChild(title);\n    }\n  }\n}\n"
    annotations:
      position: back
      yaxis:
      - y: 0
        yAxisIndex: 0
        borderColor: '#bdbdbd'
        borderWidth: 2
        strokeDashArray: 0
        label:
          text: 0 kW
          position: left
          borderColor: '#757575'
          style:
            background: '#424242'
            color: '#ffffff'
            fontSize: 10px
    legend:
      show: true
      fontSize: 12px
    grid:
      borderColor: '#424242'
      strokeDashArray: 2
    xaxis:
      labels:
        datetimeUTC: false
        format: dd.MM HH:mm
    tooltip:
      shared: false
      x:
        format: dd.MM HH:mm
  series:
  - entity: sensor.replace_with_load_power
    name: Load
    color: '#42a5f5'
    type: line
    yaxis_id: power
    unit: kW
    float_precision: 2
    stroke_width: 2
    curve: monotoneCubic
    extend_to: now
    fill_raw: 'null'
    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'
    group_by:
      func: avg
      duration: 30min
      fill: last
    show:
      legend_value: false
  - entity: sensor.replace_with_grid_power
    name: Grid
    color: '#ef5350'
    type: line
    yaxis_id: power
    unit: kW
    float_precision: 2
    stroke_width: 2
    curve: monotoneCubic
    extend_to: now
    fill_raw: 'null'
    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'
    group_by:
      func: avg
      duration: 30min
      fill: last
    show:
      legend_value: false
  - entity: sensor.replace_with_pv_power
    name: PV
    color: '#ffb300'
    type: line
    yaxis_id: power
    unit: kW
    float_precision: 2
    stroke_width: 2
    curve: monotoneCubic
    extend_to: now
    fill_raw: 'null'
    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'
    group_by:
      func: avg
      duration: 30min
      fill: last
    show:
      legend_value: false
  - entity: sensor.replace_with_battery_soc
    name: SoC
    color: '#66bb6a'
    type: line
    yaxis_id: soc
    unit: '%'
    float_precision: 0
    stroke_width: 2
    curve: monotoneCubic
    extend_to: now
    fill_raw: 'null'
    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1 : null;'
    group_by:
      func: avg
      duration: 30min
      fill: last
    show:
      legend_value: false
  - entity: sensor.replace_with_plan
    name: Load · plan
    color: '#42a5f5'
    type: line
    yaxis_id: power
    unit: kW
    float_precision: 2
    stroke_width: 2
    stroke_dash: 6
    curve: monotoneCubic
    extend_to: false
    show:
      in_header: false
      legend_value: false
    data_generator: "const now = Date.now();\nconst valid = hass.states['binary_sensor.replace_with_forecast_valid'];\n//\
      \ The integration retains the last complete generation while its replacement runs.\nconst refreshing = entity.attributes.refreshing\
      \ === true;\nif (['unknown', 'unavailable'].includes(entity.state) ||\n    (!refreshing && (valid?.state !== 'on' ||\n\
      \      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\nconst rows = (entity.attributes.intervals ||\
      \ []).filter(r =>\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\n  Date.parse(r.end)\
      \ > Date.parse(r.start) && Date.parse(r.end) > now\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\nconst\
      \ points = [];\nlet previousEnd = null;\nlet previousValue = null;\nfor (const r of rows) {\n  const from = Date.parse(r.start),\
      \ to = Date.parse(r.end);\n  const value = (Number.isFinite(r.load_kwh)) ? (r.load_kwh) / ((to - from) / 3600000) :\
      \ null;\n  // Clip the first slot at now, but divide by its original duration.\n  const left = Math.max(from, now);\n\
      \  if (previousEnd !== null && from > previousEnd) {\n    points.push([previousEnd, previousValue], [previousEnd + 1,\
      \ null]);\n  }\n  points.push([left, value]);\n  previousEnd = to;\n  previousValue = value;\n}\nif (previousEnd !==\
      \ null) points.push([previousEnd, previousValue]);\nreturn points;\n"
  - entity: sensor.replace_with_plan
    name: Grid · plan
    color: '#ef5350'
    type: line
    yaxis_id: power
    unit: kW
    float_precision: 2
    stroke_width: 2
    stroke_dash: 6
    curve: stepline
    extend_to: false
    show:
      in_header: false
      legend_value: false
    data_generator: "const now = Date.now();\nconst valid = hass.states['binary_sensor.replace_with_forecast_valid'];\n//\
      \ The integration retains the last complete generation while its replacement runs.\nconst refreshing = entity.attributes.refreshing\
      \ === true;\nif (['unknown', 'unavailable'].includes(entity.state) ||\n    (!refreshing && (valid?.state !== 'on' ||\n\
      \      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\nconst rows = (entity.attributes.intervals ||\
      \ []).filter(r =>\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\n  Date.parse(r.end)\
      \ > Date.parse(r.start) && Date.parse(r.end) > now\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\nconst\
      \ points = [];\nlet previousEnd = null;\nlet previousValue = null;\nfor (const r of rows) {\n  const from = Date.parse(r.start),\
      \ to = Date.parse(r.end);\n  const value = (Number.isFinite(r.grid_import_kwh) && Number.isFinite(r.grid_export_kwh))\
      \ ? (r.grid_import_kwh - r.grid_export_kwh) / ((to - from) / 3600000) : null;\n  // Clip the first slot at now, but\
      \ divide by its original duration.\n  const left = Math.max(from, now);\n  if (previousEnd !== null && from > previousEnd)\
      \ {\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\n  }\n  points.push([left, value]);\n \
      \ previousEnd = to;\n  previousValue = value;\n}\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\n\
      return points;\n"
  - entity: sensor.replace_with_plan
    name: PV · plan
    color: '#ffb300'
    type: line
    yaxis_id: power
    unit: kW
    float_precision: 2
    stroke_width: 2
    stroke_dash: 6
    curve: monotoneCubic
    extend_to: false
    show:
      in_header: false
      legend_value: false
    data_generator: "const now = Date.now();\nconst valid = hass.states['binary_sensor.replace_with_forecast_valid'];\n//\
      \ The integration retains the last complete generation while its replacement runs.\nconst refreshing = entity.attributes.refreshing\
      \ === true;\nif (['unknown', 'unavailable'].includes(entity.state) ||\n    (!refreshing && (valid?.state !== 'on' ||\n\
      \      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\nconst rows = (entity.attributes.intervals ||\
      \ []).filter(r =>\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\n  Date.parse(r.end)\
      \ > Date.parse(r.start) && Date.parse(r.end) > now\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\nconst\
      \ points = [];\nlet previousEnd = null;\nlet previousValue = null;\nfor (const r of rows) {\n  const from = Date.parse(r.start),\
      \ to = Date.parse(r.end);\n  const value = (Number.isFinite(r.pv_kwh)) ? (r.pv_kwh) / ((to - from) / 3600000) : null;\n\
      \  // Clip the first slot at now, but divide by its original duration.\n  const left = Math.max(from, now);\n  if (previousEnd\
      \ !== null && from > previousEnd) {\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\n  }\n\
      \  points.push([left, value]);\n  previousEnd = to;\n  previousValue = value;\n}\nif (previousEnd !== null) points.push([previousEnd,\
      \ previousValue]);\nreturn points;\n"
  - entity: sensor.replace_with_plan
    name: SoC · plan
    color: '#66bb6a'
    type: line
    yaxis_id: soc
    unit: '%'
    float_precision: 0
    stroke_width: 2
    stroke_dash: 6
    curve: monotoneCubic
    extend_to: false
    show:
      in_header: false
      legend_value: false
    data_generator: "const now = Date.now();\nconst valid = hass.states['binary_sensor.replace_with_forecast_valid'];\n//\
      \ The integration retains the last complete generation while its replacement runs.\nconst refreshing = entity.attributes.refreshing\
      \ === true;\nif (['unknown', 'unavailable'].includes(entity.state) ||\n    (!refreshing && (valid?.state !== 'on' ||\n\
      \      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\nconst rows = (entity.attributes.intervals ||\
      \ []).filter(r =>\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\n  Date.parse(r.end)\
      \ > Date.parse(r.start) && Date.parse(r.end) > now\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\nconst\
      \ points = [];\nconst capacityKwh = 25;\nconst raw = hass.states['sensor.replace_with_battery_soc']?.state;\nconst current\
      \ = raw == null || raw === '' ? NaN : Number(raw);\nlet previousEnd = null;\nfor (const r of rows) {\n  const from =\
      \ Date.parse(r.start), to = Date.parse(r.end);\n  if (previousEnd === null && from <= now && Number.isFinite(current))\
      \ {\n    points.push([now, current]);\n  } else if (previousEnd !== null && from > previousEnd) {\n    points.push([previousEnd\
      \ + 1, null]);\n  }\n  const value = Number.isFinite(r.end_soc_kwh) ? r.end_soc_kwh / capacityKwh * 100 : null;\n  points.push([to,\
      \ value]);\n  previousEnd = to;\n}\nreturn points;\n"
- type: markdown
  grid_options:
    columns: full
    rows: auto
  content: '**Solid lines:** measurements up to now · **Dashed:** Energy Compass plan from now.

    12 h history (power and SoC: 30 min means) + 24 h ahead. **Grid:** + import / − export. Plan SoC: capacity 25 kWh.


    **Background (14% opacity):** CHARGE_PV yellow · CHARGE_GRID red · DISCHARGE_GRID purple · SELF_CONSUME green.

    HOLD and other states gray. Narrow intervals: hover for the label.


    {% set valid = ''binary_sensor.replace_with_forecast_valid'' %}

    {% set plan = ''sensor.replace_with_plan'' %}

    {% set until = state_attr(plan, ''valid_until'') %}

    {% if has_value(plan) and state_attr(plan, ''refreshing'') == true %}

    **Recalculating - showing the previous plan. The new one replaces it automatically.**

    {% elif is_state(valid, ''on'') and until and as_timestamp(until, 0) > as_timestamp(now()) %}

    {% set coverage = state_attr(valid, ''coverage_end'') %}

    {% if coverage %}Plan available until **{{ as_local(as_datetime(coverage)).strftime(''%d.%m %H:%M'') }}**.{% endif %}

    {% if not state_attr(valid, ''coverage_complete'') %} The rest stays empty - forecasts do not cover it completely.{% endif
    %}

    {% else %}

    **Forecast stale or unavailable - only measured history is shown.**

    {% endif %}

    '
