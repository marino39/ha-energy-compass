// Generated by tools/build_builder.py - do not edit; edit the generators and rebuild.
window.EC_BUILDER = {
 "patterns": {
  "entity": "^[a-z_]+\\.[a-z0-9_]+$",
  "number": "^[0-9]+(\\.[0-9]+)?$",
  "prefix": "^[a-z0-9_]+$",
  "signed": "^-?[0-9]+(\\.[0-9]+)?$",
  "tariff_group": "^(G11|G12|G12w)$",
  "afternoon_window": "^(fixed|seasonal)$"
 },
 "fields": {
  "plan": {
   "token": "__EC_PLAN__",
   "kind": "entity",
   "example": "sensor.energy_compass_plan",
   "label": {
    "en": "Plan",
    "pl": "Plan"
   }
  },
  "valid": {
   "token": "__EC_VALID__",
   "kind": "entity",
   "example": "binary_sensor.energy_compass_forecast_valid",
   "label": {
    "en": "Forecast valid",
    "pl": "Poprawna prognoza"
   }
  },
  "consumer": {
   "token": "__EC_CONSUMER__",
   "kind": "entity",
   "example": "sensor.energy_compass_consumer_compass",
   "label": {
    "en": "Consumer compass",
    "pl": "Kompas zużycia"
   }
  },
  "soc": {
   "token": "__EC_SOC__",
   "kind": "entity",
   "example": "sensor.inverter_deye_battery",
   "label": {
    "en": "Battery SOC (%)",
    "pl": "SOC baterii (%)"
   }
  },
  "load": {
   "token": "__EC_LOAD__",
   "kind": "entity",
   "example": "sensor.inverter_deye_load_power",
   "label": {
    "en": "Load power (W)",
    "pl": "Moc obciążenia (W)"
   }
  },
  "grid": {
   "token": "__EC_GRID__",
   "kind": "entity",
   "example": "sensor.inverter_deye_grid_power",
   "label": {
    "en": "Grid power (W, + import)",
    "pl": "Moc sieci (W, + import)"
   }
  },
  "pv": {
   "token": "__EC_PV__",
   "kind": "entity",
   "example": "sensor.inverter_deye_pv_power",
   "label": {
    "en": "PV power (W)",
    "pl": "Moc PV (W)"
   }
  },
  "alert": {
   "token": "__EC_ALERT__",
   "kind": "entity",
   "example": "binary_sensor.energy_compass_alert",
   "label": {
    "en": "Alert",
    "pl": "Alert"
   }
  },
  "optimizer": {
   "token": "__EC_OPTIMIZER__",
   "kind": "entity",
   "example": "sensor.energy_compass_optimizer_status",
   "label": {
    "en": "Optimizer status",
    "pl": "Stan optymalizatora"
   }
  },
  "machine": {
   "token": "__EC_MACHINE__",
   "kind": "entity",
   "example": "sensor.energy_compass_energy_compass",
   "label": {
    "en": "Energy compass (planned battery state)",
    "pl": "Kompas energii (planowany stan baterii)"
   }
  },
  "consumer_cost": {
   "token": "__EC_CONSUMER_COST__",
   "kind": "entity",
   "example": "sensor.energy_compass_consumption_cost",
   "label": {
    "en": "Consumption cost",
    "pl": "Koszt zużycia"
   }
  },
  "next_change": {
   "token": "__EC_NEXT_CHANGE__",
   "kind": "entity",
   "example": "sensor.energy_compass_next_change",
   "label": {
    "en": "Next change",
    "pl": "Następna zmiana"
   }
  },
  "battery_power": {
   "token": "__EC_BATTERY_POWER__",
   "kind": "entity",
   "example": "sensor.inverter_deye_battery_power",
   "label": {
    "en": "Battery power (W, + discharge)",
    "pl": "Moc baterii (W, + rozładowanie)"
   }
  },
  "battery_state": {
   "token": "__EC_BATTERY_STATE__",
   "kind": "entity",
   "example": "sensor.inverter_deye_battery_state",
   "label": {
    "en": "Battery state (inverter)",
    "pl": "Stan baterii (falownik)"
   }
  },
  "charge_limit": {
   "token": "__EC_CHARGE_LIMIT__",
   "kind": "entity",
   "example": "number.inverter_deye_battery_max_charging_current",
   "label": {
    "en": "Battery max charging current",
    "pl": "Maks. prąd ładowania baterii"
   }
  },
  "discharge_limit": {
   "token": "__EC_DISCHARGE_LIMIT__",
   "kind": "entity",
   "example": "number.inverter_deye_battery_max_discharging_current",
   "label": {
    "en": "Battery max discharging current",
    "pl": "Maks. prąd rozładowania baterii"
   }
  },
  "grid_limit": {
   "token": "__EC_GRID_LIMIT__",
   "kind": "entity",
   "example": "number.inverter_deye_battery_grid_charging_current",
   "label": {
    "en": "Battery grid charging current",
    "pl": "Prąd ładowania baterii z sieci"
   }
  },
  "tariff_group": {
   "token": "__EC_TARIFF_GROUP__",
   "kind": "tariff_group",
   "example": "G12",
   "label": {
    "en": "Tariff group",
    "pl": "Grupa taryfowa"
   },
   "options": [
    "G11",
    "G12",
    "G12w"
   ]
  },
  "base_rate": {
   "token": "__EC_BASE_RATE__",
   "kind": "number",
   "example": "1.25",
   "label": {
    "en": "Peak (or G11) final price per kWh",
    "pl": "Cena szczytowa (lub G11) za kWh, brutto"
   }
  },
  "off_peak_rate": {
   "token": "__EC_OFF_PEAK_RATE__",
   "kind": "number",
   "example": "0.61",
   "label": {
    "en": "Off-peak final price per kWh",
    "pl": "Cena pozaszczytowa za kWh, brutto"
   }
  },
  "afternoon_window": {
   "token": "__EC_AFTERNOON_WINDOW__",
   "kind": "afternoon_window",
   "example": "fixed",
   "label": {
    "en": "Afternoon off-peak window: fixed 13-15 or seasonal (15-17 Apr-Sep)",
    "pl": "Popołudniowe okno taniej strefy: stałe 13-15 lub sezonowe (15-17 IV-IX)"
   },
   "options": [
    "fixed",
    "seasonal"
   ]
  },
  "multiplier": {
   "token": "__EC_MULTIPLIER__",
   "kind": "number",
   "example": "1.23",
   "label": {
    "en": "Sell price multiplier (net-billing deposit 1.23)",
    "pl": "Mnożnik ceny sprzedaży (depozyt net-billing 1,23)"
   }
  },
  "floor": {
   "token": "__EC_FLOOR__",
   "kind": "signed",
   "example": "0",
   "label": {
    "en": "Sell price floor in PLN/MWh (net-billing 0)",
    "pl": "Minimalna cena sprzedaży w PLN/MWh (net-billing 0)"
   }
  },
  "import_cost": {
   "token": "__EC_IMPORT_COST__",
   "kind": "entity",
   "example": "sensor.inverter_total_energy_import_cost",
   "label": {
    "en": "Grid import cost (Energy dashboard cost sensor)",
    "pl": "Koszt importu z sieci (sensor kosztu z panelu Energia)"
   }
  },
  "import_energy": {
   "token": "__EC_IMPORT_ENERGY__",
   "kind": "entity",
   "example": "sensor.inverter_total_energy_import",
   "label": {
    "en": "Grid import energy (kWh, total)",
    "pl": "Energia pobrana z sieci (kWh, licznik)"
   }
  },
  "export_energy": {
   "token": "__EC_EXPORT_ENERGY__",
   "kind": "entity",
   "example": "sensor.inverter_total_energy_export",
   "label": {
    "en": "Grid export energy (kWh, total)",
    "pl": "Energia oddana do sieci (kWh, licznik)"
   }
  },
  "import_price": {
   "token": "__EC_IMPORT_PRICE__",
   "kind": "entity",
   "example": "sensor.energy_compass_tariff_price",
   "label": {
    "en": "Current buy price (for gaps after a restart)",
    "pl": "Bieżąca cena zakupu (do luk po restarcie)"
   }
  },
  "export_prices": {
   "token": "__EC_EXPORT_PRICES__",
   "kind": "entity",
   "example": "sensor.energy_compass_rce_export_forecast",
   "label": {
    "en": "Export price forecast (prices attribute)",
    "pl": "Prognoza ceny sprzedaży (atrybut prices)"
   }
  },
  "deposit": {
   "token": "__EC_DEPOSIT__",
   "kind": "entity",
   "example": "sensor.export_value",
   "label": {
    "en": "Export value sensor (from the counter)",
    "pl": "Sensor wartości eksportu (z licznika)"
   }
  },
  "capacity": {
   "token": "__EC_CAPACITY__",
   "kind": "number",
   "example": "25",
   "label": {
    "en": "Battery capacity used by Energy Compass (kWh)",
    "pl": "Pojemność baterii w Energy Compass (kWh)"
   }
  },
  "prefix": {
   "token": "__EC_PREFIX__",
   "kind": "prefix",
   "example": "inverter_deye_program_",
   "label": {
    "en": "TOU program entity prefix",
    "pl": "Prefiks encji programów TOU"
   }
  }
 },
 "sections": {
  "plan": {
   "label": {
    "en": "Plan chart (ApexCharts)",
    "pl": "Wykres planu (ApexCharts)"
   }
  },
  "consumer": {
   "label": {
    "en": "Consumer Compass timeline (ApexCharts)",
    "pl": "Oś czasu Consumer Compass (ApexCharts)"
   }
  },
  "panel": {
   "label": {
    "en": "Deye controller panel",
    "pl": "Panel sterownika Deye"
   }
  },
  "diagnostics": {
   "label": {
    "en": "Deye controller diagnostics",
    "pl": "Diagnostyka sterownika Deye"
   }
  },
  "cost": {
   "label": {
    "en": "Cost card (purchase, deposit, balance)",
    "pl": "Karta kosztów (zakup, depozyt, bilans)"
   }
  },
  "package": {
   "label": {
    "en": "Deye controller package",
    "pl": "Pakiet sterownika Deye"
   }
  },
  "tariff": {
   "label": {
    "en": "Buy price: G11/G12/G12w tariff",
    "pl": "Cena zakupu: taryfa G11/G12/G12w"
   }
  },
  "rce": {
   "label": {
    "en": "Sell price: RCE (net-billing)",
    "pl": "Cena sprzedaży: RCE (net-billing)"
   }
  }
 },
 "templates": {
  "plan": {
   "en": {
    "yaml": "type: grid\ncolumn_span: 4\ncards:\n- type: heading\n  heading: Energy Compass\n  heading_style: title\n  icon: mdi:chart-timeline-variant\n- type: custom:apexcharts-card\n  section_mode: true\n  grid_options:\n    columns: full\n    rows: 9\n  header:\n    show: true\n    title: Load / Grid / PV / SoC - history and plan\n    show_states: true\n    colorize_states: true\n  graph_span: 36h\n  span:\n    offset: +24h\n  update_interval: 1min\n  hours_12: false\n  now:\n    show: true\n    label: Now\n    color: '#9e9e9e'\n  show:\n    last_updated: true\n  yaxis:\n  - id: power\n    decimals: 1\n    min: ~0\n    max: ~0\n    apex_config:\n      tickAmount: 6\n      title:\n        text: Power [kW]\n  - id: soc\n    opposite: true\n    min: 0\n    max: 100\n    decimals: 0\n    apex_config:\n      tickAmount: 5\n      title:\n        text: SoC [%]\n  apex_config:\n    chart:\n      animations:\n        enabled: false\n      toolbar:\n        show: false\n      events:\n        mounted: \"EVAL:function (chart) {\\n  // ApexCharts Card 2.2.3 keeps the latest HA state on its shadow-root host.\\n  const hass = chart.el.getRootNode().host?._hass;\\n  for (const id of chart.__energyCompassAnnotationIds || []) chart.removeAnnotation(id);\\n  chart.__energyCompassAnnotationIds = [];\\n  const plan = hass?.states['__EC_PLAN__'];\\n  const valid = hass?.states['__EC_VALID__'];\\n  const now = Date.now();\\n  if (!plan || ['unknown', 'unavailable'].includes(plan.state)) return;\\n  if (plan.attributes.refreshing !== true &&\\n      (valid?.state !== 'on' || !(Date.parse(plan.attributes.valid_until) > now))) return;\\n\\n  const min = Math.max(now, chart.w.globals.minX);\\n  const max = chart.w.globals.maxX;\\n  if (!Number.isFinite(min) || !Number.isFinite(max) || max <= min) return;\\n  const stateColors = {\\n    CHARGE_PV: '#fdd835',\\n    CHARGE_GRID: '#ef5350',\\n    DISCHARGE_GRID: '#ab47bc',\\n    SELF_CONSUME: '#66bb6a',\\n  };\\n  const intervals = (plan.attributes.intervals || []).map(row => ({\\n    start: Math.max(min, Date.parse(row.start)), end: Math.min(max, Date.parse(row.end)),\\n    state: row.state,\\n  })).filter(row => Number.isFinite(row.start) && row.end > row.start)\\n    .sort((a, b) => a.start - b.start);\\n  const runs = [];\\n  for (const interval of intervals) {\\n    const lastRun = runs[runs.length - 1];\\n    if (lastRun && lastRun.state === interval.state && lastRun.end === interval.start) {\\n      lastRun.end = interval.end;\\n    } else runs.push({...interval});\\n  }\\n\\n  const add = (annotation) => {\\n    chart.addXaxisAnnotation(annotation, false);\\n    chart.__energyCompassAnnotationIds.push(annotation.id);\\n  };\\n  runs.forEach((band, index) => add({\\n    id: 'ec-background-' + index,\\n    x: band.start, x2: band.end,\\n    fillColor: stateColors[band.state] || '#78909c', opacity: 0.14,\\n    borderColor: 'transparent', strokeDashArray: 0,\\n  }));\\n\\n  runs.forEach((run, index) => {\\n    const width = (run.end - run.start) / (max - chart.w.globals.minX) * chart.w.globals.gridWidth;\\n    const label = width >= 13 ? {\\n      text: run.state, position: 'top', orientation: 'vertical', offsetY: 8, offsetX: width / 2,\\n      borderWidth: 0,\\n      style: {background: 'transparent', color: hass?.themes?.darkMode === false ? '#616161' : '#bdbdbd', fontSize: '9px', fontWeight: 700,\\n        padding: {left: 1, right: 1, top: 1, bottom: 1}},\\n    } : {text: ''};\\n    add({id: 'ec-state-' + index, x: run.start, x2: run.end,\\n      fillColor: 'transparent', opacity: 0, borderColor: '#616161', strokeDashArray: 3, label});\\n  });\\n\\n  // Native SVG titles expose even intervals too narrow for a visible label.\\n  for (const [index, run] of runs.entries()) {\\n    const time = stamp => new Date(stamp).toLocaleTimeString('en-GB', {hour: '2-digit', minute: '2-digit'});\\n    const elements = chart.el.querySelectorAll('.ec-state-' + index);\\n    for (const element of elements) {\\n      const title = document.createElementNS('http://www.w3.org/2000/svg', 'title');\\n      title.textContent = `${run.state} · ${time(run.start)}–${time(run.end)}`;\\n      element.appendChild(title);\\n    }\\n  }\\n}\\n\"\n        updated: \"EVAL:function (chart) {\\n  // ApexCharts Card 2.2.3 keeps the latest HA state on its shadow-root host.\\n  const hass = chart.el.getRootNode().host?._hass;\\n  for (const id of chart.__energyCompassAnnotationIds || []) chart.removeAnnotation(id);\\n  chart.__energyCompassAnnotationIds = [];\\n  const plan = hass?.states['__EC_PLAN__'];\\n  const valid = hass?.states['__EC_VALID__'];\\n  const now = Date.now();\\n  if (!plan || ['unknown', 'unavailable'].includes(plan.state)) return;\\n  if (plan.attributes.refreshing !== true &&\\n      (valid?.state !== 'on' || !(Date.parse(plan.attributes.valid_until) > now))) return;\\n\\n  const min = Math.max(now, chart.w.globals.minX);\\n  const max = chart.w.globals.maxX;\\n  if (!Number.isFinite(min) || !Number.isFinite(max) || max <= min) return;\\n  const stateColors = {\\n    CHARGE_PV: '#fdd835',\\n    CHARGE_GRID: '#ef5350',\\n    DISCHARGE_GRID: '#ab47bc',\\n    SELF_CONSUME: '#66bb6a',\\n  };\\n  const intervals = (plan.attributes.intervals || []).map(row => ({\\n    start: Math.max(min, Date.parse(row.start)), end: Math.min(max, Date.parse(row.end)),\\n    state: row.state,\\n  })).filter(row => Number.isFinite(row.start) && row.end > row.start)\\n    .sort((a, b) => a.start - b.start);\\n  const runs = [];\\n  for (const interval of intervals) {\\n    const lastRun = runs[runs.length - 1];\\n    if (lastRun && lastRun.state === interval.state && lastRun.end === interval.start) {\\n      lastRun.end = interval.end;\\n    } else runs.push({...interval});\\n  }\\n\\n  const add = (annotation) => {\\n    chart.addXaxisAnnotation(annotation, false);\\n    chart.__energyCompassAnnotationIds.push(annotation.id);\\n  };\\n  runs.forEach((band, index) => add({\\n    id: 'ec-background-' + index,\\n    x: band.start, x2: band.end,\\n    fillColor: stateColors[band.state] || '#78909c', opacity: 0.14,\\n    borderColor: 'transparent', strokeDashArray: 0,\\n  }));\\n\\n  runs.forEach((run, index) => {\\n    const width = (run.end - run.start) / (max - chart.w.globals.minX) * chart.w.globals.gridWidth;\\n    const label = width >= 13 ? {\\n      text: run.state, position: 'top', orientation: 'vertical', offsetY: 8, offsetX: width / 2,\\n      borderWidth: 0,\\n      style: {background: 'transparent', color: hass?.themes?.darkMode === false ? '#616161' : '#bdbdbd', fontSize: '9px', fontWeight: 700,\\n        padding: {left: 1, right: 1, top: 1, bottom: 1}},\\n    } : {text: ''};\\n    add({id: 'ec-state-' + index, x: run.start, x2: run.end,\\n      fillColor: 'transparent', opacity: 0, borderColor: '#616161', strokeDashArray: 3, label});\\n  });\\n\\n  // Native SVG titles expose even intervals too narrow for a visible label.\\n  for (const [index, run] of runs.entries()) {\\n    const time = stamp => new Date(stamp).toLocaleTimeString('en-GB', {hour: '2-digit', minute: '2-digit'});\\n    const elements = chart.el.querySelectorAll('.ec-state-' + index);\\n    for (const element of elements) {\\n      const title = document.createElementNS('http://www.w3.org/2000/svg', 'title');\\n      title.textContent = `${run.state} · ${time(run.start)}–${time(run.end)}`;\\n      element.appendChild(title);\\n    }\\n  }\\n}\\n\"\n    annotations:\n      position: back\n      yaxis:\n      - y: 0\n        yAxisIndex: 0\n        borderColor: '#bdbdbd'\n        borderWidth: 2\n        strokeDashArray: 0\n        label:\n          text: 0 kW\n          position: left\n          borderColor: '#757575'\n          style:\n            background: '#424242'\n            color: '#ffffff'\n            fontSize: 10px\n    legend:\n      show: true\n      fontSize: 12px\n    grid:\n      borderColor: '#424242'\n      strokeDashArray: 2\n    xaxis:\n      labels:\n        datetimeUTC: false\n        format: dd.MM HH:mm\n    tooltip:\n      shared: false\n      x:\n        format: dd.MM HH:mm\n  series:\n  - entity: __EC_LOAD__\n    name: Load\n    color: '#42a5f5'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_GRID__\n    name: Grid\n    color: '#ef5350'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_PV__\n    name: PV\n    color: '#ffb300'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_SOC__\n    name: SoC\n    color: '#66bb6a'\n    type: line\n    yaxis_id: soc\n    unit: '%'\n    float_precision: 0\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_PLAN__\n    name: Load · plan\n    color: '#42a5f5'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    stroke_dash: 6\n    curve: monotoneCubic\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nlet previousEnd = null;\\nlet previousValue = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  const value = (Number.isFinite(r.load_kwh)) ? (r.load_kwh) / ((to - from) / 3600000) : null;\\n  // Clip the first slot at now, but divide by its original duration.\\n  const left = Math.max(from, now);\\n  if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\\n  }\\n  points.push([left, value]);\\n  previousEnd = to;\\n  previousValue = value;\\n}\\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\\nreturn points;\\n\"\n  - entity: __EC_PLAN__\n    name: Grid · plan\n    color: '#ef5350'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    stroke_dash: 6\n    curve: stepline\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nlet previousEnd = null;\\nlet previousValue = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  const value = (Number.isFinite(r.grid_import_kwh) && Number.isFinite(r.grid_export_kwh)) ? (r.grid_import_kwh - r.grid_export_kwh) / ((to - from) / 3600000) : null;\\n  // Clip the first slot at now, but divide by its original duration.\\n  const left = Math.max(from, now);\\n  if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\\n  }\\n  points.push([left, value]);\\n  previousEnd = to;\\n  previousValue = value;\\n}\\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\\nreturn points;\\n\"\n  - entity: __EC_PLAN__\n    name: PV · plan\n    color: '#ffb300'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    stroke_dash: 6\n    curve: monotoneCubic\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nlet previousEnd = null;\\nlet previousValue = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  const value = (Number.isFinite(r.pv_kwh)) ? (r.pv_kwh) / ((to - from) / 3600000) : null;\\n  // Clip the first slot at now, but divide by its original duration.\\n  const left = Math.max(from, now);\\n  if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\\n  }\\n  points.push([left, value]);\\n  previousEnd = to;\\n  previousValue = value;\\n}\\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\\nreturn points;\\n\"\n  - entity: __EC_PLAN__\n    name: SoC · plan\n    color: '#66bb6a'\n    type: line\n    yaxis_id: soc\n    unit: '%'\n    float_precision: 0\n    stroke_width: 2\n    stroke_dash: 6\n    curve: monotoneCubic\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nconst capacityKwh = __EC_CAPACITY__;\\nconst raw = hass.states['__EC_SOC__']?.state;\\nconst current = raw == null || raw === '' ? NaN : Number(raw);\\nlet previousEnd = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  if (previousEnd === null && from <= now && Number.isFinite(current)) {\\n    points.push([now, current]);\\n  } else if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd + 1, null]);\\n  }\\n  const value = Number.isFinite(r.end_soc_kwh) ? r.end_soc_kwh / capacityKwh * 100 : null;\\n  points.push([to, value]);\\n  previousEnd = to;\\n}\\nreturn points;\\n\"\n- type: markdown\n  grid_options:\n    columns: full\n    rows: auto\n  content: '**Solid lines:** measurements up to now · **Dashed:** Energy Compass plan from now.\n\n    12 h history (power and SoC: 30 min means) + 24 h ahead. **Grid:** + import / − export. Plan SoC: capacity __EC_CAPACITY__ kWh.\n\n\n    **Background (14% opacity):** CHARGE_PV yellow · CHARGE_GRID red · DISCHARGE_GRID purple · SELF_CONSUME green.\n\n    HOLD and other states gray. Narrow intervals: hover for the label.\n\n\n    {% set valid = ''__EC_VALID__'' %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set until = state_attr(plan, ''valid_until'') %}\n\n    {% if has_value(plan) and state_attr(plan, ''refreshing'') == true %}\n\n    **Recalculating - showing the previous plan. The new one replaces it automatically.**\n\n    {% elif is_state(valid, ''on'') and until and as_timestamp(until, 0) > as_timestamp(now()) %}\n\n    {% set coverage = state_attr(valid, ''coverage_end'') %}\n\n    {% if coverage %}Plan available until **{{ as_local(as_datetime(coverage)).strftime(''%d.%m %H:%M'') }}**.{% endif %}\n\n    {% if not state_attr(valid, ''coverage_complete'') %} The rest stays empty - forecasts do not cover it completely.{% endif %}\n\n    {% else %}\n\n    **Forecast stale or unavailable - only measured history is shown.**\n\n    {% endif %}\n\n    '\n",
    "fields": [
     "plan",
     "valid",
     "soc",
     "load",
     "grid",
     "pv",
     "capacity"
    ]
   },
   "pl": {
    "yaml": "type: grid\ncolumn_span: 4\ncards:\n- type: heading\n  heading: Energy Compass\n  heading_style: title\n  icon: mdi:chart-timeline-variant\n- type: custom:apexcharts-card\n  section_mode: true\n  grid_options:\n    columns: full\n    rows: 9\n  header:\n    show: true\n    title: Load / Grid / PV / SoC — historia i projekcja\n    show_states: true\n    colorize_states: true\n  graph_span: 36h\n  span:\n    offset: +24h\n  update_interval: 1min\n  hours_12: false\n  now:\n    show: true\n    label: Teraz\n    color: '#9e9e9e'\n  show:\n    last_updated: true\n  yaxis:\n  - id: power\n    decimals: 1\n    min: ~0\n    max: ~0\n    apex_config:\n      tickAmount: 6\n      title:\n        text: Moc [kW]\n  - id: soc\n    opposite: true\n    min: 0\n    max: 100\n    decimals: 0\n    apex_config:\n      tickAmount: 5\n      title:\n        text: SoC [%]\n  apex_config:\n    chart:\n      animations:\n        enabled: false\n      toolbar:\n        show: false\n      events:\n        mounted: \"EVAL:function (chart) {\\n  // ApexCharts Card 2.2.3 keeps the latest HA state on its shadow-root host.\\n  const hass = chart.el.getRootNode().host?._hass;\\n  for (const id of chart.__energyCompassAnnotationIds || []) chart.removeAnnotation(id);\\n  chart.__energyCompassAnnotationIds = [];\\n  const plan = hass?.states['__EC_PLAN__'];\\n  const valid = hass?.states['__EC_VALID__'];\\n  const now = Date.now();\\n  if (!plan || ['unknown', 'unavailable'].includes(plan.state)) return;\\n  if (plan.attributes.refreshing !== true &&\\n      (valid?.state !== 'on' || !(Date.parse(plan.attributes.valid_until) > now))) return;\\n\\n  const min = Math.max(now, chart.w.globals.minX);\\n  const max = chart.w.globals.maxX;\\n  if (!Number.isFinite(min) || !Number.isFinite(max) || max <= min) return;\\n  const stateColors = {\\n    CHARGE_PV: '#fdd835',\\n    CHARGE_GRID: '#ef5350',\\n    DISCHARGE_GRID: '#ab47bc',\\n    SELF_CONSUME: '#66bb6a',\\n  };\\n  const intervals = (plan.attributes.intervals || []).map(row => ({\\n    start: Math.max(min, Date.parse(row.start)), end: Math.min(max, Date.parse(row.end)),\\n    state: row.state,\\n  })).filter(row => Number.isFinite(row.start) && row.end > row.start)\\n    .sort((a, b) => a.start - b.start);\\n  const runs = [];\\n  for (const interval of intervals) {\\n    const lastRun = runs[runs.length - 1];\\n    if (lastRun && lastRun.state === interval.state && lastRun.end === interval.start) {\\n      lastRun.end = interval.end;\\n    } else runs.push({...interval});\\n  }\\n\\n  const add = (annotation) => {\\n    chart.addXaxisAnnotation(annotation, false);\\n    chart.__energyCompassAnnotationIds.push(annotation.id);\\n  };\\n  runs.forEach((band, index) => add({\\n    id: 'ec-background-' + index,\\n    x: band.start, x2: band.end,\\n    fillColor: stateColors[band.state] || '#78909c', opacity: 0.14,\\n    borderColor: 'transparent', strokeDashArray: 0,\\n  }));\\n\\n  runs.forEach((run, index) => {\\n    const width = (run.end - run.start) / (max - chart.w.globals.minX) * chart.w.globals.gridWidth;\\n    const label = width >= 13 ? {\\n      text: run.state, position: 'top', orientation: 'vertical', offsetY: 8, offsetX: width / 2,\\n      borderWidth: 0,\\n      style: {background: 'transparent', color: hass?.themes?.darkMode === false ? '#616161' : '#bdbdbd', fontSize: '9px', fontWeight: 700,\\n        padding: {left: 1, right: 1, top: 1, bottom: 1}},\\n    } : {text: ''};\\n    add({id: 'ec-state-' + index, x: run.start, x2: run.end,\\n      fillColor: 'transparent', opacity: 0, borderColor: '#616161', strokeDashArray: 3, label});\\n  });\\n\\n  // Native SVG titles expose even intervals too narrow for a visible label.\\n  for (const [index, run] of runs.entries()) {\\n    const time = stamp => new Date(stamp).toLocaleTimeString('pl-PL', {hour: '2-digit', minute: '2-digit'});\\n    const elements = chart.el.querySelectorAll('.ec-state-' + index);\\n    for (const element of elements) {\\n      const title = document.createElementNS('http://www.w3.org/2000/svg', 'title');\\n      title.textContent = `${run.state} · ${time(run.start)}–${time(run.end)}`;\\n      element.appendChild(title);\\n    }\\n  }\\n}\\n\"\n        updated: \"EVAL:function (chart) {\\n  // ApexCharts Card 2.2.3 keeps the latest HA state on its shadow-root host.\\n  const hass = chart.el.getRootNode().host?._hass;\\n  for (const id of chart.__energyCompassAnnotationIds || []) chart.removeAnnotation(id);\\n  chart.__energyCompassAnnotationIds = [];\\n  const plan = hass?.states['__EC_PLAN__'];\\n  const valid = hass?.states['__EC_VALID__'];\\n  const now = Date.now();\\n  if (!plan || ['unknown', 'unavailable'].includes(plan.state)) return;\\n  if (plan.attributes.refreshing !== true &&\\n      (valid?.state !== 'on' || !(Date.parse(plan.attributes.valid_until) > now))) return;\\n\\n  const min = Math.max(now, chart.w.globals.minX);\\n  const max = chart.w.globals.maxX;\\n  if (!Number.isFinite(min) || !Number.isFinite(max) || max <= min) return;\\n  const stateColors = {\\n    CHARGE_PV: '#fdd835',\\n    CHARGE_GRID: '#ef5350',\\n    DISCHARGE_GRID: '#ab47bc',\\n    SELF_CONSUME: '#66bb6a',\\n  };\\n  const intervals = (plan.attributes.intervals || []).map(row => ({\\n    start: Math.max(min, Date.parse(row.start)), end: Math.min(max, Date.parse(row.end)),\\n    state: row.state,\\n  })).filter(row => Number.isFinite(row.start) && row.end > row.start)\\n    .sort((a, b) => a.start - b.start);\\n  const runs = [];\\n  for (const interval of intervals) {\\n    const lastRun = runs[runs.length - 1];\\n    if (lastRun && lastRun.state === interval.state && lastRun.end === interval.start) {\\n      lastRun.end = interval.end;\\n    } else runs.push({...interval});\\n  }\\n\\n  const add = (annotation) => {\\n    chart.addXaxisAnnotation(annotation, false);\\n    chart.__energyCompassAnnotationIds.push(annotation.id);\\n  };\\n  runs.forEach((band, index) => add({\\n    id: 'ec-background-' + index,\\n    x: band.start, x2: band.end,\\n    fillColor: stateColors[band.state] || '#78909c', opacity: 0.14,\\n    borderColor: 'transparent', strokeDashArray: 0,\\n  }));\\n\\n  runs.forEach((run, index) => {\\n    const width = (run.end - run.start) / (max - chart.w.globals.minX) * chart.w.globals.gridWidth;\\n    const label = width >= 13 ? {\\n      text: run.state, position: 'top', orientation: 'vertical', offsetY: 8, offsetX: width / 2,\\n      borderWidth: 0,\\n      style: {background: 'transparent', color: hass?.themes?.darkMode === false ? '#616161' : '#bdbdbd', fontSize: '9px', fontWeight: 700,\\n        padding: {left: 1, right: 1, top: 1, bottom: 1}},\\n    } : {text: ''};\\n    add({id: 'ec-state-' + index, x: run.start, x2: run.end,\\n      fillColor: 'transparent', opacity: 0, borderColor: '#616161', strokeDashArray: 3, label});\\n  });\\n\\n  // Native SVG titles expose even intervals too narrow for a visible label.\\n  for (const [index, run] of runs.entries()) {\\n    const time = stamp => new Date(stamp).toLocaleTimeString('pl-PL', {hour: '2-digit', minute: '2-digit'});\\n    const elements = chart.el.querySelectorAll('.ec-state-' + index);\\n    for (const element of elements) {\\n      const title = document.createElementNS('http://www.w3.org/2000/svg', 'title');\\n      title.textContent = `${run.state} · ${time(run.start)}–${time(run.end)}`;\\n      element.appendChild(title);\\n    }\\n  }\\n}\\n\"\n    annotations:\n      position: back\n      yaxis:\n      - y: 0\n        yAxisIndex: 0\n        borderColor: '#bdbdbd'\n        borderWidth: 2\n        strokeDashArray: 0\n        label:\n          text: 0 kW\n          position: left\n          borderColor: '#757575'\n          style:\n            background: '#424242'\n            color: '#ffffff'\n            fontSize: 10px\n    legend:\n      show: true\n      fontSize: 12px\n    grid:\n      borderColor: '#424242'\n      strokeDashArray: 2\n    xaxis:\n      labels:\n        datetimeUTC: false\n        format: dd.MM HH:mm\n    tooltip:\n      shared: false\n      x:\n        format: dd.MM HH:mm\n  series:\n  - entity: __EC_LOAD__\n    name: Load\n    color: '#42a5f5'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_GRID__\n    name: Grid\n    color: '#ef5350'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_PV__\n    name: PV\n    color: '#ffb300'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1000 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_SOC__\n    name: SoC\n    color: '#66bb6a'\n    type: line\n    yaxis_id: soc\n    unit: '%'\n    float_precision: 0\n    stroke_width: 2\n    curve: monotoneCubic\n    extend_to: now\n    fill_raw: 'null'\n    transform: 'if (x === null || x === '''') return null; const v = Number(x); return Number.isFinite(v) ? v / 1 : null;'\n    group_by:\n      func: avg\n      duration: 30min\n      fill: last\n    show:\n      legend_value: false\n  - entity: __EC_PLAN__\n    name: Load · plan\n    color: '#42a5f5'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    stroke_dash: 6\n    curve: monotoneCubic\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nlet previousEnd = null;\\nlet previousValue = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  const value = (Number.isFinite(r.load_kwh)) ? (r.load_kwh) / ((to - from) / 3600000) : null;\\n  // Clip the first slot at now, but divide by its original duration.\\n  const left = Math.max(from, now);\\n  if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\\n  }\\n  points.push([left, value]);\\n  previousEnd = to;\\n  previousValue = value;\\n}\\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\\nreturn points;\\n\"\n  - entity: __EC_PLAN__\n    name: Grid · plan\n    color: '#ef5350'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    stroke_dash: 6\n    curve: stepline\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nlet previousEnd = null;\\nlet previousValue = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  const value = (Number.isFinite(r.grid_import_kwh) && Number.isFinite(r.grid_export_kwh)) ? (r.grid_import_kwh - r.grid_export_kwh) / ((to - from) / 3600000) : null;\\n  // Clip the first slot at now, but divide by its original duration.\\n  const left = Math.max(from, now);\\n  if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\\n  }\\n  points.push([left, value]);\\n  previousEnd = to;\\n  previousValue = value;\\n}\\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\\nreturn points;\\n\"\n  - entity: __EC_PLAN__\n    name: PV · plan\n    color: '#ffb300'\n    type: line\n    yaxis_id: power\n    unit: kW\n    float_precision: 2\n    stroke_width: 2\n    stroke_dash: 6\n    curve: monotoneCubic\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nlet previousEnd = null;\\nlet previousValue = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  const value = (Number.isFinite(r.pv_kwh)) ? (r.pv_kwh) / ((to - from) / 3600000) : null;\\n  // Clip the first slot at now, but divide by its original duration.\\n  const left = Math.max(from, now);\\n  if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd, previousValue], [previousEnd + 1, null]);\\n  }\\n  points.push([left, value]);\\n  previousEnd = to;\\n  previousValue = value;\\n}\\nif (previousEnd !== null) points.push([previousEnd, previousValue]);\\nreturn points;\\n\"\n  - entity: __EC_PLAN__\n    name: SoC · plan\n    color: '#66bb6a'\n    type: line\n    yaxis_id: soc\n    unit: '%'\n    float_precision: 0\n    stroke_width: 2\n    stroke_dash: 6\n    curve: monotoneCubic\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst valid = hass.states['__EC_VALID__'];\\n// The integration retains the last complete generation while its replacement runs.\\nconst refreshing = entity.attributes.refreshing === true;\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!refreshing && (valid?.state !== 'on' ||\\n      !(Date.parse(entity.attributes.valid_until) > now)))) return [];\\nconst rows = (entity.attributes.intervals || []).filter(r =>\\n  Number.isFinite(Date.parse(r.start)) && Number.isFinite(Date.parse(r.end)) &&\\n  Date.parse(r.end) > Date.parse(r.start) && Date.parse(r.end) > now\\n).sort((a, b) => Date.parse(a.start) - Date.parse(b.start));\\nconst points = [];\\nconst capacityKwh = __EC_CAPACITY__;\\nconst raw = hass.states['__EC_SOC__']?.state;\\nconst current = raw == null || raw === '' ? NaN : Number(raw);\\nlet previousEnd = null;\\nfor (const r of rows) {\\n  const from = Date.parse(r.start), to = Date.parse(r.end);\\n  if (previousEnd === null && from <= now && Number.isFinite(current)) {\\n    points.push([now, current]);\\n  } else if (previousEnd !== null && from > previousEnd) {\\n    points.push([previousEnd + 1, null]);\\n  }\\n  const value = Number.isFinite(r.end_soc_kwh) ? r.end_soc_kwh / capacityKwh * 100 : null;\\n  points.push([to, value]);\\n  previousEnd = to;\\n}\\nreturn points;\\n\"\n- type: markdown\n  grid_options:\n    columns: full\n    rows: auto\n  content: '**Linie ciągłe:** pomiary do teraz · **Przerywane:** plan Energy Compass od teraz.\n\n    12 h historii (moc i SoC: średnie 30 min) + 24 h naprzód. **Grid:** + import / − eksport. SoC planu: pojemność __EC_CAPACITY__ kWh.\n\n\n    **Tło (14% krycia):** CHARGE_PV — żółty · CHARGE_GRID — czerwony · DISCHARGE_GRID — fioletowy · SELF_CONSUME — zielony.\n\n    HOLD i pozostałe stany — szary. Krótkie przedziały: opis po najechaniu.\n\n\n    {% set valid = ''__EC_VALID__'' %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set until = state_attr(plan, ''valid_until'') %}\n\n    {% if has_value(plan) and state_attr(plan, ''refreshing'') == true %}\n\n    **Przeliczanie — wyświetlany poprzedni plan. Nowy zastąpi go automatycznie.**\n\n    {% elif is_state(valid, ''on'') and until and as_timestamp(until, 0) > as_timestamp(now()) %}\n\n    {% set coverage = state_attr(valid, ''coverage_end'') %}\n\n    {% if coverage %}Plan dostępny do **{{ as_local(as_datetime(coverage)).strftime(''%d.%m %H:%M'') }}**.{% endif %}\n\n    {% if not state_attr(valid, ''coverage_complete'') %} Dalsza część pozostaje pusta — brak pełnego pokrycia prognoz.{% endif %}\n\n    {% else %}\n\n    **Prognoza nieaktualna lub niedostępna — widoczna tylko historia pomiarów.**\n\n    {% endif %}\n\n    '\n",
    "fields": [
     "plan",
     "valid",
     "soc",
     "load",
     "grid",
     "pv",
     "capacity"
    ]
   }
  },
  "consumer": {
   "en": {
    "yaml": "type: grid\ncolumn_span: 4\ncards:\n- type: custom:apexcharts-card\n  section_mode: true\n  grid_options:\n    columns: full\n    rows: 4\n  header:\n    show: true\n    title: Consumer Compass - history and forecast\n    show_states: false\n  graph_span: 24h\n  span:\n    offset: +20h\n  update_interval: 1min\n  hours_12: false\n  now:\n    show: true\n    label: Now\n    color: '#ffb300'\n  yaxis:\n  - min: 0\n    max: 1\n    show: false\n  apex_config:\n    chart:\n      animations:\n        enabled: false\n      toolbar:\n        show: false\n      zoom:\n        enabled: false\n    grid:\n      show: false\n      padding:\n        top: 10\n        bottom: 0\n        left: 8\n        right: 8\n    markers:\n      size: 0\n    dataLabels:\n      enabled: false\n    xaxis:\n      labels:\n        datetimeUTC: false\n        format: HH:mm\n        hideOverlappingLabels: true\n      tooltip:\n        enabled: false\n      axisBorder:\n        show: false\n      axisTicks:\n        show: false\n    legend:\n      show: true\n      position: bottom\n      fontSize: 12px\n      customLegendItems:\n      - BOOST\n      - CHEAP\n      - NORMAL\n      - LIMIT\n      markers:\n        fillColors:\n        - '#2e7d32'\n        - '#66bb6a'\n        - '#78909c'\n        - '#c62828'\n      onItemClick:\n        toggleDataSeries: false\n      onItemHover:\n        highlightDataSeries: false\n    tooltip:\n      shared: false\n      intersect: false\n      custom: \"EVAL:function({seriesIndex, dataPointIndex, w}) {\\n  const value = w.globals.series[seriesIndex]?.[dataPointIndex];\\n  if (value !== 1) return '';\\n  const timestamp = w.globals.seriesX[seriesIndex]?.[dataPointIndex];\\n  if (!Number.isFinite(timestamp)) return '';\\n  const name = w.globals.seriesNames[seriesIndex];\\n  const date = new Date(timestamp).toLocaleString('en-GB', {day:'2-digit', month:'2-digit', hour:'2-digit', minute:'2-digit'});\\n  return '<div style=\\\"padding:8px 12px\\\"><b>' + name + '</b><br>' + date + '</div>';\\n}\"\n  series:\n  - entity: __EC_CONSUMER__\n    name: BOOST · history\n    color: '#2e7d32'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''BOOST'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_CONSUMER__\n    name: CHEAP · history\n    color: '#66bb6a'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''CHEAP'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_CONSUMER__\n    name: NORMAL · history\n    color: '#78909c'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''NORMAL'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_CONSUMER__\n    name: LIMIT · history\n    color: '#c62828'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''LIMIT'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_PLAN__\n    name: BOOST · forecast\n    color: '#2e7d32'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'BOOST' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n  - entity: __EC_PLAN__\n    name: CHEAP · forecast\n    color: '#66bb6a'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'CHEAP' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n  - entity: __EC_PLAN__\n    name: NORMAL · forecast\n    color: '#78909c'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'NORMAL' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n  - entity: __EC_PLAN__\n    name: LIMIT · forecast\n    color: '#c62828'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'LIMIT' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n- type: markdown\n  grid_options:\n    columns: full\n    rows: auto\n  content: '**Now: {{ states(''__EC_CONSUMER__'') }}** · 4 h history + 20 h ahead.\n\n    Solid colors: history · Lighter: forecast · Empty: no data.\n\n    {% if state_attr(''__EC_PLAN__'', ''refreshing'') == true %}\n\n    Recalculating - showing the last available forecast.\n\n    {% elif not is_state(''__EC_VALID__'', ''on'') %}\n\n    Forecast unavailable - history is shown.\n\n    {% endif %}'\n",
    "fields": [
     "plan",
     "valid",
     "consumer"
    ]
   },
   "pl": {
    "yaml": "type: grid\ncolumn_span: 4\ncards:\n- type: custom:apexcharts-card\n  section_mode: true\n  grid_options:\n    columns: full\n    rows: 4\n  header:\n    show: true\n    title: Consumer Compass — historia i prognoza\n    show_states: false\n  graph_span: 24h\n  span:\n    offset: +20h\n  update_interval: 1min\n  hours_12: false\n  now:\n    show: true\n    label: Teraz\n    color: '#ffb300'\n  yaxis:\n  - min: 0\n    max: 1\n    show: false\n  apex_config:\n    chart:\n      animations:\n        enabled: false\n      toolbar:\n        show: false\n      zoom:\n        enabled: false\n    grid:\n      show: false\n      padding:\n        top: 10\n        bottom: 0\n        left: 8\n        right: 8\n    markers:\n      size: 0\n    dataLabels:\n      enabled: false\n    xaxis:\n      labels:\n        datetimeUTC: false\n        format: HH:mm\n        hideOverlappingLabels: true\n      tooltip:\n        enabled: false\n      axisBorder:\n        show: false\n      axisTicks:\n        show: false\n    legend:\n      show: true\n      position: bottom\n      fontSize: 12px\n      customLegendItems:\n      - BOOST\n      - CHEAP\n      - NORMAL\n      - LIMIT\n      markers:\n        fillColors:\n        - '#2e7d32'\n        - '#66bb6a'\n        - '#78909c'\n        - '#c62828'\n      onItemClick:\n        toggleDataSeries: false\n      onItemHover:\n        highlightDataSeries: false\n    tooltip:\n      shared: false\n      intersect: false\n      custom: \"EVAL:function({seriesIndex, dataPointIndex, w}) {\\n  const value = w.globals.series[seriesIndex]?.[dataPointIndex];\\n  if (value !== 1) return '';\\n  const timestamp = w.globals.seriesX[seriesIndex]?.[dataPointIndex];\\n  if (!Number.isFinite(timestamp)) return '';\\n  const name = w.globals.seriesNames[seriesIndex];\\n  const date = new Date(timestamp).toLocaleString('pl-PL', {day:'2-digit', month:'2-digit', hour:'2-digit', minute:'2-digit'});\\n  return '<div style=\\\"padding:8px 12px\\\"><b>' + name + '</b><br>' + date + '</div>';\\n}\"\n  series:\n  - entity: __EC_CONSUMER__\n    name: BOOST · historia\n    color: '#2e7d32'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''BOOST'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_CONSUMER__\n    name: CHEAP · historia\n    color: '#66bb6a'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''CHEAP'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_CONSUMER__\n    name: NORMAL · historia\n    color: '#78909c'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''NORMAL'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_CONSUMER__\n    name: LIMIT · historia\n    color: '#c62828'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 1\n    extend_to: now\n    show:\n      in_header: false\n      legend_value: false\n    transform: 'return x === ''LIMIT'' ? 1 : 0;'\n    fill_raw: zero\n  - entity: __EC_PLAN__\n    name: BOOST · prognoza\n    color: '#2e7d32'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'BOOST' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n  - entity: __EC_PLAN__\n    name: CHEAP · prognoza\n    color: '#66bb6a'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'CHEAP' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n  - entity: __EC_PLAN__\n    name: NORMAL · prognoza\n    color: '#78909c'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'NORMAL' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n  - entity: __EC_PLAN__\n    name: LIMIT · prognoza\n    color: '#c62828'\n    type: area\n    curve: stepline\n    stroke_width: 0\n    opacity: 0.55\n    extend_to: false\n    show:\n      in_header: false\n      legend_value: false\n    data_generator: \"const now = Date.now();\\nconst a = entity.attributes;\\nconst valid = hass.states['__EC_VALID__'];\\nif (['unknown', 'unavailable'].includes(entity.state) ||\\n    (!a.refreshing && (valid?.state !== 'on' || !(Date.parse(a.valid_until) > now)))) return [];\\nconst left = Math.max(now, start.getTime());\\nconst coverage = Date.parse(valid?.attributes?.coverage_end);\\nconst right = Math.min(end.getTime(), Number.isFinite(coverage) ? coverage : end.getTime());\\nif (right <= left) return [];\\nconst points = new Map([[left, 0], [right, 0]]);\\nfor (const row of (a.outlook || [])) {\\n  const from = Math.max(left, Date.parse(row.start));\\n  const to = Math.min(right, Date.parse(row.end));\\n  if (!(to > from) || !['BOOST', 'CHEAP', 'NORMAL', 'LIMIT'].includes(row.level)) continue;\\n  const value = row.level === 'LIMIT' ? 1 : 0;\\n  points.set(from, value);\\n  points.set(to - 1, value);\\n  points.set(to, 0);\\n}\\nreturn [...points].sort((a, b) => a[0] - b[0]);\\n\"\n- type: markdown\n  grid_options:\n    columns: full\n    rows: auto\n  content: '**Teraz: {{ states(''__EC_CONSUMER__'') }}** · 4 h historii + 20 h naprzód.\n\n    Pełne kolory: historia · Jaśniejsze: prognoza · Puste miejsce: brak danych.\n\n    {% if state_attr(''__EC_PLAN__'', ''refreshing'') == true %}\n\n    Przeliczanie — widoczna ostatnia dostępna prognoza.\n\n    {% elif not is_state(''__EC_VALID__'', ''on'') %}\n\n    Prognoza niedostępna — widoczna historia.\n\n    {% endif %}'\n",
    "fields": [
     "plan",
     "valid",
     "consumer"
    ]
   }
  },
  "panel": {
   "en": {
    "yaml": "type: grid\ncolumn_span: 2\ncards:\n- type: heading\n  heading: Battery\n  heading_style: title\n  icon: mdi:home-battery\n- type: conditional\n  grid_options:\n    columns: 24\n    rows: auto\n  conditions:\n  - condition: or\n    conditions:\n    - condition: state\n      entity: __EC_VALID__\n      state_not: 'on'\n    - condition: state\n      entity: __EC_ALERT__\n      state: 'on'\n    - condition: and\n      conditions:\n      - condition: state\n        entity: input_select.energy_compass_deye_mode\n        state: Auto\n      - condition: state\n        entity: sensor.energy_compass_deye_runtime\n        state_not: ok\n  card:\n    type: markdown\n    title: Check control\n    content: '{% set labels = {''CHARGE_PV'':''Charging from PV'',''CHARGE_GRID'':''Charging from grid'',''DISCHARGE_GRID'':''Selling to grid'',''SELF_CONSUME'':''Powering the house'',''HOLD'':''Holding'',''CURTAIL'':''Holding (PV curtailment)'',''BASE'':''Base mode''} %}\n\n      {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n      {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n      {% set plan = ''__EC_PLAN__'' %}\n\n      {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n      {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n      {% if not valid %}⚠️ The forecast is not current. The schedule is not a current recommendation.{% endif %}\n\n      {% if is_state(''__EC_ALERT__'',''on'') %}\n\n      ⚠️ {{ state_attr(''__EC_ALERT__'',''reason'') or ''The Compass reports a problem.'' }}\n\n      {% endif %}\n\n      {% if is_state(''input_select.energy_compass_deye_mode'',''Auto'') %}\n\n      {% if rt.get(''code'') != ''ok'' %}⚠️ Controller: {{ rt.get(''reason'') or rt.get(''code'') or ''No data'' }}{% endif %}\n\n      {% if not fresh %}⚠️ No fresh confirmation of the inverter settings.{% endif %}\n\n      {% if rt.get(''uncertain'') %}⚠️ Unconfirmed writes: {{ rt.get(''uncertain'')|length }}.{% endif %}\n\n      {% if rt.get(''warning'') %}⚠️ {{ rt.warning }}{% endif %}\n\n      {% endif %}'\n    grid_options:\n      columns: 24\n      rows: auto\n- type: vertical-stack\n  grid_options:\n    columns: 12\n    rows: auto\n  cards:\n  - type: horizontal-stack\n    cards:\n    - type: tile\n      entity: __EC_SOC__\n      name: Charge\n      color: green\n    - type: tile\n      entity: __EC_BATTERY_POWER__\n      name: Battery power\n      color: purple\n  - type: markdown\n    title: Battery now\n    content: '{% set labels = {''CHARGE_PV'':''Charging from PV'',''CHARGE_GRID'':''Charging from grid'',''DISCHARGE_GRID'':''Selling to grid'',''SELF_CONSUME'':''Powering the house'',''HOLD'':''Holding'',''CURTAIL'':''Holding (PV curtailment)'',''BASE'':''Base mode''} %}\n\n      {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n      {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n      {% set plan = ''__EC_PLAN__'' %}\n\n      {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n      {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n      {% set power = states(''__EC_BATTERY_POWER__'') %}\n\n      {% if is_number(power) %}\n\n      **{{ ''Discharging'' if power|float > 50 else ''Charging'' if power|float < -50 else ''Idle'' }} · {{ (power|float|abs / 1000)|round(2) }} kW**\n\n      {% else %}\n\n      No current power reading.\n\n      {% endif %}\n\n\n      Control: **{{ {''Auto'':''Automatic'',''Simulation'':''Simulation'',''Off'':''Off''}.get(states(''input_select.energy_compass_deye_mode''),''Unavailable'') }}**\n\n\n      {% if is_state(''input_select.energy_compass_deye_mode'',''Auto'') and fresh and rt.get(''code'') == ''ok'' and not rt.get(''uncertain'') %}\n\n      Settings confirmed: **{{ labels.get(rt.get(''confirmed_mode''),''No mode confirmation'') }}**\n\n      {% else %}\n\n      Controller status: **{{ rt.get(''reason'') or rt.get(''code'') or ''No data'' }}**\n\n      {% endif %}'\n    grid_options:\n      columns: 12\n      rows: auto\n  - type: tile\n    entity: input_select.energy_compass_deye_mode\n    name: Control mode\n    icon: mdi:compass\n    features:\n    - type: select-options\n- type: markdown\n  title: Compass decision\n  content: '{% set labels = {''CHARGE_PV'':''Charging from PV'',''CHARGE_GRID'':''Charging from grid'',''DISCHARGE_GRID'':''Selling to grid'',''SELF_CONSUME'':''Powering the house'',''HOLD'':''Holding'',''CURTAIL'':''Holding (PV curtailment)'',''BASE'':''Base mode''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    {% set mode = states(''__EC_MACHINE__'') %}\n\n    {% if valid %}\n\n    ### {{ colors.get(mode,''⚪'') }} {{ labels.get(mode,''No recommendation'') }}\n\n    {% set ns = namespace(row=none) %}\n\n    {% for row in state_attr(plan,''intervals'') or [] %}\n\n    {% if as_timestamp(row.start,0) <= as_timestamp(now()) < as_timestamp(row.end,0) %}{% set ns.row = row %}{% endif %}\n\n    {% endfor %}\n\n    {% if ns.row %}\n\n    Current segment until **{{ as_local(as_datetime(ns.row.end)).strftime(''%H:%M'') }}**.\n\n    {% endif %}\n\n    {% else %}\n\n    **No current recommendation.** The plan expired or the forecast is unavailable.\n\n    {% endif %}\n\n\n    {% set n = rt.get(''active_tou'',0)|int %}\n\n    {% set confirmed = rt.get(''confirmed'') or {} %}\n\n    {% set prefix = state_attr(''sensor.energy_compass_deye_tou_settings'',''prefix'') or '''' %}\n\n    {% set target = confirmed.get(''number.'' ~ prefix ~ n ~ ''_soc'') %}\n\n    {% if is_state(''input_select.energy_compass_deye_mode'',''Auto'') and fresh and rt.get(''code'') == ''ok'' and not rt.get(''uncertain'') and target is not none %}\n\n    {{ ''SOC target'' if rt.get(''confirmed_mode'') == ''CHARGE_GRID'' else ''SOC floor'' }} of the controller: **{{ target|round(0)|int }}%**\n\n    {% endif %}\n\n\n    {% if valid %}\n\n    {% set ns = namespace(next=none) %}\n\n    {% for row in state_attr(plan,''intervals'') or [] %}\n\n    {% if not ns.next and as_timestamp(row.start,0) > as_timestamp(now()) and row.state != mode %}{% set ns.next = row %}{% endif %}\n\n    {% endfor %}\n\n    {% if ns.next %}\n\n    Then: **{{ labels.get(ns.next.state,ns.next.state) }}**, {{ as_local(as_datetime(ns.next.start)).strftime(''%d.%m %H:%M'') }}.\n\n    {% endif %}\n\n    {% endif %}\n\n\n    Power shows what the battery actually does. The recommendation and confirmation refer to the plan and the settings.'\n  grid_options:\n    columns: 12\n    rows: auto\n- type: heading\n  heading: Battery plan\n  heading_style: subtitle\n  icon: mdi:chart-timeline-variant\n- type: markdown\n  title: Next 24 hours\n  content: '{% set labels = {''CHARGE_PV'':''Charging from PV'',''CHARGE_GRID'':''Charging from grid'',''DISCHARGE_GRID'':''Selling to grid'',''SELF_CONSUME'':''Powering the house'',''HOLD'':''Holding'',''CURTAIL'':''Holding (PV curtailment)'',''BASE'':''Base mode''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    {% if valid %}\n\n    {% set ns = namespace(rows=[], start=none, end=none, mode=none, soc=none) %}\n\n    {% for r in state_attr(plan,''intervals'') or [] %}\n\n    {% if as_timestamp(r.end,0) > as_timestamp(now()) and as_timestamp(r.start,0) < as_timestamp(now())+86400 %}\n\n    {% if ns.mode != r.state %}\n\n    {% if ns.mode is not none %}{% set ns.rows = ns.rows + [{''start'':ns.start,''end'':ns.end,''mode'':ns.mode,''soc'':ns.soc}] %}{% endif %}\n\n    {% set ns.start = r.start %}{% set ns.mode = r.state %}\n\n    {% endif %}\n\n    {% set ns.end = r.end %}{% set ns.soc = r.end_soc_kwh %}\n\n    {% endif %}\n\n    {% endfor %}\n\n    {% if ns.mode is not none %}{% set ns.rows = ns.rows + [{''start'':ns.start,''end'':ns.end,''mode'':ns.mode,''soc'':ns.soc}] %}{% endif %}\n\n    | From | Battery plan | SOC at the end |\n\n    |:--|:--|--:|\n\n    {% for r in ns.rows %}| {{ ''Now'' if as_timestamp(r.start,0) <= as_timestamp(now()) else as_local(as_datetime(r.start)).strftime(''%d.%m %H:%M'') }} | {{ colors.get(r.mode,''⚪'') }} {{ labels.get(r.mode,r.mode) }} | {{ (r.soc|float / __EC_CAPACITY__ * 100)|round(0)|int }}% |\n\n    {% endfor %}\n\n\n    The plan can change after new measurements. SOC is computed for the configured __EC_CAPACITY__ kWh.\n\n    {% else %}\n\n    Schedule unavailable - waiting for a current forecast.\n\n    {% endif %}'\n  grid_options:\n    columns: 12\n    rows: auto\n- type: custom:apexcharts-card\n  grid_options:\n    columns: 12\n    rows: auto\n  header:\n    show: true\n    title: Charge forecast · 24 h\n  graph_span: 24h\n  span:\n    start: minute\n  update_interval: 1min\n  now:\n    show: true\n    label: Now\n  hours_12: false\n  yaxis:\n  - min: 0\n    max: 100\n    decimals: 0\n  apex_config:\n    chart:\n      height: 260\n      toolbar:\n        show: false\n    legend:\n      show: false\n    xaxis:\n      labels:\n        datetimeUTC: false\n    tooltip:\n      x:\n        format: dd.MM HH:mm\n  series:\n  - entity: __EC_PLAN__\n    name: SOC forecast\n    unit: '%'\n    type: area\n    color: '#43a047'\n    opacity: 0.15\n    stroke_width: 2\n    curve: straight\n    extend_to: false\n    float_precision: 0\n    data_generator: 'const a = entity.attributes, now = Date.now();\n\n      if (hass.states[''__EC_VALID__'']?.state !== ''on'' || !(Date.parse(a.valid_until) > now)) return [];\n\n      const rows = (a.intervals || []).filter(r => Date.parse(r.end) > now && Date.parse(r.end) <= now + 86400000);\n\n      const points = rows.map(r => [Date.parse(r.end), Math.max(0, Math.min(100, Number(r.end_soc_kwh) / __EC_CAPACITY__ * 100))]);\n\n      const soc = hass.states[''__EC_SOC__''];\n\n      const reported = Date.parse(soc?.attributes?.reported_at ?? soc?.last_updated);\n\n      if (soc && Number.isFinite(Number(soc.state)) && now - reported < 90000) points.unshift([now, Number(soc.state)]);\n\n      return points;'\n- type: markdown\n  title: Household consumption compass\n  content: '{% set labels = {''CHARGE_PV'':''Charging from PV'',''CHARGE_GRID'':''Charging from grid'',''DISCHARGE_GRID'':''Selling to grid'',''SELF_CONSUME'':''Powering the house'',''HOLD'':''Holding'',''CURTAIL'':''Holding (PV curtailment)'',''BASE'':''Base mode''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    {% if valid %}\n\n    {% set level = states(''__EC_CONSUMER__'') %}\n\n    {% set cost = states(''__EC_CONSUMER_COST__'') %}\n\n    **{{ {''BOOST'':''🟢 Use more'',''CHEAP'':''🟢 Cheap use'',''NORMAL'':''⚪ Normal use'',''LIMIT'':''🔴 Limit use''}.get(level,''No recommendation'') }}**\n\n    {% if is_number(cost) %} · {{ cost|float|round(3) }} PLN/kWh of extra use{% endif %}\n\n\n    {% set change = states(''__EC_NEXT_CHANGE__'') %}\n\n    {% if as_timestamp(change,0) > as_timestamp(now()) %}\n\n    Next change: **{{ {''BOOST'':''use more'',''CHEAP'':''cheap use'',''NORMAL'':''normal use'',''LIMIT'':''limit use''}.get(state_attr(''__EC_NEXT_CHANGE__'',''next_level''),''No data'') }}**, {{ as_local(as_datetime(change)).strftime(''%d.%m %H:%M'') }}.\n\n    {% endif %}\n\n\n    Estimated cost of extra energy used in the house, with the battery taken into account. This is not the grid purchase price.\n\n    {% else %}\n\n    No consumption hint - no current forecast.\n\n    {% endif %}'\n  grid_options:\n    columns: 24\n    rows: auto\n- type: heading\n  heading: History\n  heading_style: subtitle\n  icon: mdi:history\n- type: sensor\n  entity: __EC_SOC__\n  name: Charge · last 24 h\n  graph: line\n  hours_to_show: 24\n  detail: 2\n  grid_options:\n    columns: 12\n    rows: auto\n- type: sensor\n  entity: __EC_BATTERY_POWER__\n  name: Power · + discharge / − charge\n  graph: line\n  hours_to_show: 24\n  detail: 2\n  grid_options:\n    columns: 12\n    rows: auto\n- type: history-graph\n  title: Control history · 24 h\n  hours_to_show: 24\n  grid_options:\n    columns: 24\n    rows: auto\n  entities:\n  - entity: input_select.energy_compass_deye_mode\n    name: Control\n  - entity: __EC_MACHINE__\n    name: Battery recommendation\n  - entity: sensor.energy_compass_deye_runtime\n    name: Controller state\n  - entity: __EC_BATTERY_STATE__\n    name: Battery operation\n",
    "fields": [
     "plan",
     "valid",
     "consumer",
     "soc",
     "alert",
     "machine",
     "consumer_cost",
     "next_change",
     "battery_power",
     "battery_state",
     "capacity"
    ]
   },
   "pl": {
    "yaml": "type: grid\ncolumn_span: 2\ncards:\n- type: heading\n  heading: Bateria\n  heading_style: title\n  icon: mdi:home-battery\n- type: conditional\n  grid_options:\n    columns: 24\n    rows: auto\n  conditions:\n  - condition: or\n    conditions:\n    - condition: state\n      entity: __EC_VALID__\n      state_not: 'on'\n    - condition: state\n      entity: __EC_ALERT__\n      state: 'on'\n    - condition: and\n      conditions:\n      - condition: state\n        entity: input_select.energy_compass_deye_mode\n        state: Auto\n      - condition: state\n        entity: sensor.energy_compass_deye_runtime\n        state_not: ok\n  card:\n    type: markdown\n    title: Sprawdź sterowanie\n    content: '{% set labels = {''CHARGE_PV'':''Ładowanie z PV'',''CHARGE_GRID'':''Ładowanie z sieci'',''DISCHARGE_GRID'':''Sprzedaż do sieci'',''SELF_CONSUME'':''Zasilanie domu'',''HOLD'':''Podtrzymanie'',''CURTAIL'':''Podtrzymanie (ograniczenie PV)'',''BASE'':''Tryb bazowy''} %}\n\n      {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n      {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n      {% set plan = ''__EC_PLAN__'' %}\n\n      {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n      {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n      {% if not valid %}⚠️ Prognoza nie jest aktualna. Harmonogram nie stanowi bieżącego zalecenia.{% endif %}\n\n      {% if is_state(''__EC_ALERT__'',''on'') %}\n\n      ⚠️ {{ state_attr(''__EC_ALERT__'',''reason'') or ''Kompas zgłasza problem.'' }}\n\n      {% endif %}\n\n      {% if is_state(''input_select.energy_compass_deye_mode'',''Auto'') %}\n\n      {% if rt.get(''code'') != ''ok'' %}⚠️ Sterownik: {{ rt.get(''reason'') or rt.get(''code'') or ''Brak danych'' }}{% endif %}\n\n      {% if not fresh %}⚠️ Brak świeżego potwierdzenia ustawień falownika.{% endif %}\n\n      {% if rt.get(''uncertain'') %}⚠️ Niepotwierdzone zapisy: {{ rt.get(''uncertain'')|length }}.{% endif %}\n\n      {% if rt.get(''warning'') %}⚠️ {{ rt.warning }}{% endif %}\n\n      {% endif %}'\n    grid_options:\n      columns: 24\n      rows: auto\n- type: vertical-stack\n  grid_options:\n    columns: 12\n    rows: auto\n  cards:\n  - type: horizontal-stack\n    cards:\n    - type: tile\n      entity: __EC_SOC__\n      name: Naładowanie\n      color: green\n    - type: tile\n      entity: __EC_BATTERY_POWER__\n      name: Moc baterii\n      color: purple\n  - type: markdown\n    title: Bateria teraz\n    content: '{% set labels = {''CHARGE_PV'':''Ładowanie z PV'',''CHARGE_GRID'':''Ładowanie z sieci'',''DISCHARGE_GRID'':''Sprzedaż do sieci'',''SELF_CONSUME'':''Zasilanie domu'',''HOLD'':''Podtrzymanie'',''CURTAIL'':''Podtrzymanie (ograniczenie PV)'',''BASE'':''Tryb bazowy''} %}\n\n      {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n      {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n      {% set plan = ''__EC_PLAN__'' %}\n\n      {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n      {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n      {% set power = states(''__EC_BATTERY_POWER__'') %}\n\n      {% if is_number(power) %}\n\n      **{{ ''Rozładowanie'' if power|float > 50 else ''Ładowanie'' if power|float < -50 else ''Spoczynek'' }} · {{ (power|float|abs / 1000)|round(2) }} kW**\n\n      {% else %}\n\n      Brak aktualnego pomiaru mocy.\n\n      {% endif %}\n\n\n      Sterowanie: **{{ {''Auto'':''Automatyczne'',''Simulation'':''Symulacja'',''Off'':''Wyłączone''}.get(states(''input_select.energy_compass_deye_mode''),''Niedostępne'') }}**\n\n\n      {% if is_state(''input_select.energy_compass_deye_mode'',''Auto'') and fresh and rt.get(''code'') == ''ok'' and not rt.get(''uncertain'') %}\n\n      Ustawienia potwierdzone: **{{ labels.get(rt.get(''confirmed_mode''),''Brak potwierdzenia trybu'') }}**\n\n      {% else %}\n\n      Status sterownika: **{{ rt.get(''reason'') or rt.get(''code'') or ''Brak danych'' }}**\n\n      {% endif %}'\n    grid_options:\n      columns: 12\n      rows: auto\n  - type: tile\n    entity: input_select.energy_compass_deye_mode\n    name: Tryb sterowania\n    icon: mdi:compass\n    features:\n    - type: select-options\n- type: markdown\n  title: Decyzja Kompasu\n  content: '{% set labels = {''CHARGE_PV'':''Ładowanie z PV'',''CHARGE_GRID'':''Ładowanie z sieci'',''DISCHARGE_GRID'':''Sprzedaż do sieci'',''SELF_CONSUME'':''Zasilanie domu'',''HOLD'':''Podtrzymanie'',''CURTAIL'':''Podtrzymanie (ograniczenie PV)'',''BASE'':''Tryb bazowy''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    {% set mode = states(''__EC_MACHINE__'') %}\n\n    {% if valid %}\n\n    ### {{ colors.get(mode,''⚪'') }} {{ labels.get(mode,''Brak zalecenia'') }}\n\n    {% set ns = namespace(row=none) %}\n\n    {% for row in state_attr(plan,''intervals'') or [] %}\n\n    {% if as_timestamp(row.start,0) <= as_timestamp(now()) < as_timestamp(row.end,0) %}{% set ns.row = row %}{% endif %}\n\n    {% endfor %}\n\n    {% if ns.row %}\n\n    Bieżący odcinek do **{{ as_local(as_datetime(ns.row.end)).strftime(''%H:%M'') }}**.\n\n    {% endif %}\n\n    {% else %}\n\n    **Brak aktualnego zalecenia.** Plan wygasł lub prognoza jest niedostępna.\n\n    {% endif %}\n\n\n    {% set n = rt.get(''active_tou'',0)|int %}\n\n    {% set confirmed = rt.get(''confirmed'') or {} %}\n\n    {% set prefix = state_attr(''sensor.energy_compass_deye_tou_settings'',''prefix'') or '''' %}\n\n    {% set target = confirmed.get(''number.'' ~ prefix ~ n ~ ''_soc'') %}\n\n    {% if is_state(''input_select.energy_compass_deye_mode'',''Auto'') and fresh and rt.get(''code'') == ''ok'' and not rt.get(''uncertain'') and target is not none %}\n\n    {{ ''Cel SOC'' if rt.get(''confirmed_mode'') == ''CHARGE_GRID'' else ''Próg SOC'' }} sterownika: **{{ target|round(0)|int }}%**\n\n    {% endif %}\n\n\n    {% if valid %}\n\n    {% set ns = namespace(next=none) %}\n\n    {% for row in state_attr(plan,''intervals'') or [] %}\n\n    {% if not ns.next and as_timestamp(row.start,0) > as_timestamp(now()) and row.state != mode %}{% set ns.next = row %}{% endif %}\n\n    {% endfor %}\n\n    {% if ns.next %}\n\n    Następnie: **{{ labels.get(ns.next.state,ns.next.state) }}**, {{ as_local(as_datetime(ns.next.start)).strftime(''%d.%m %H:%M'') }}.\n\n    {% endif %}\n\n    {% endif %}\n\n\n    Pomiar mocy pokazuje rzeczywistą pracę baterii. Zalecenie i potwierdzenie dotyczą planu oraz ustawień.'\n  grid_options:\n    columns: 12\n    rows: auto\n- type: heading\n  heading: Plan baterii\n  heading_style: subtitle\n  icon: mdi:chart-timeline-variant\n- type: markdown\n  title: Najbliższe 24 godziny\n  content: '{% set labels = {''CHARGE_PV'':''Ładowanie z PV'',''CHARGE_GRID'':''Ładowanie z sieci'',''DISCHARGE_GRID'':''Sprzedaż do sieci'',''SELF_CONSUME'':''Zasilanie domu'',''HOLD'':''Podtrzymanie'',''CURTAIL'':''Podtrzymanie (ograniczenie PV)'',''BASE'':''Tryb bazowy''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    {% if valid %}\n\n    {% set ns = namespace(rows=[], start=none, end=none, mode=none, soc=none) %}\n\n    {% for r in state_attr(plan,''intervals'') or [] %}\n\n    {% if as_timestamp(r.end,0) > as_timestamp(now()) and as_timestamp(r.start,0) < as_timestamp(now())+86400 %}\n\n    {% if ns.mode != r.state %}\n\n    {% if ns.mode is not none %}{% set ns.rows = ns.rows + [{''start'':ns.start,''end'':ns.end,''mode'':ns.mode,''soc'':ns.soc}] %}{% endif %}\n\n    {% set ns.start = r.start %}{% set ns.mode = r.state %}\n\n    {% endif %}\n\n    {% set ns.end = r.end %}{% set ns.soc = r.end_soc_kwh %}\n\n    {% endif %}\n\n    {% endfor %}\n\n    {% if ns.mode is not none %}{% set ns.rows = ns.rows + [{''start'':ns.start,''end'':ns.end,''mode'':ns.mode,''soc'':ns.soc}] %}{% endif %}\n\n    | Od | Plan baterii | SOC na koniec |\n\n    |:--|:--|--:|\n\n    {% for r in ns.rows %}| {{ ''Teraz'' if as_timestamp(r.start,0) <= as_timestamp(now()) else as_local(as_datetime(r.start)).strftime(''%d.%m %H:%M'') }} | {{ colors.get(r.mode,''⚪'') }} {{ labels.get(r.mode,r.mode) }} | {{ (r.soc|float / __EC_CAPACITY__ * 100)|round(0)|int }}% |\n\n    {% endfor %}\n\n\n    Plan może się zmienić po nowych pomiarach. SOC wyliczony dla skonfigurowanych __EC_CAPACITY__ kWh.\n\n    {% else %}\n\n    Harmonogram niedostępny — oczekiwanie na aktualną prognozę.\n\n    {% endif %}'\n  grid_options:\n    columns: 12\n    rows: auto\n- type: custom:apexcharts-card\n  grid_options:\n    columns: 12\n    rows: auto\n  header:\n    show: true\n    title: Prognoza naładowania · 24 h\n  graph_span: 24h\n  span:\n    start: minute\n  update_interval: 1min\n  now:\n    show: true\n    label: Teraz\n  hours_12: false\n  yaxis:\n  - min: 0\n    max: 100\n    decimals: 0\n  apex_config:\n    chart:\n      height: 260\n      toolbar:\n        show: false\n    legend:\n      show: false\n    xaxis:\n      labels:\n        datetimeUTC: false\n    tooltip:\n      x:\n        format: dd.MM HH:mm\n  series:\n  - entity: __EC_PLAN__\n    name: Prognoza SOC\n    unit: '%'\n    type: area\n    color: '#43a047'\n    opacity: 0.15\n    stroke_width: 2\n    curve: straight\n    extend_to: false\n    float_precision: 0\n    data_generator: 'const a = entity.attributes, now = Date.now();\n\n      if (hass.states[''__EC_VALID__'']?.state !== ''on'' || !(Date.parse(a.valid_until) > now)) return [];\n\n      const rows = (a.intervals || []).filter(r => Date.parse(r.end) > now && Date.parse(r.end) <= now + 86400000);\n\n      const points = rows.map(r => [Date.parse(r.end), Math.max(0, Math.min(100, Number(r.end_soc_kwh) / __EC_CAPACITY__ * 100))]);\n\n      const soc = hass.states[''__EC_SOC__''];\n\n      const reported = Date.parse(soc?.attributes?.reported_at ?? soc?.last_updated);\n\n      if (soc && Number.isFinite(Number(soc.state)) && now - reported < 90000) points.unshift([now, Number(soc.state)]);\n\n      return points;'\n- type: markdown\n  title: Kompas zużycia w domu\n  content: '{% set labels = {''CHARGE_PV'':''Ładowanie z PV'',''CHARGE_GRID'':''Ładowanie z sieci'',''DISCHARGE_GRID'':''Sprzedaż do sieci'',''SELF_CONSUME'':''Zasilanie domu'',''HOLD'':''Podtrzymanie'',''CURTAIL'':''Podtrzymanie (ograniczenie PV)'',''BASE'':''Tryb bazowy''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    {% if valid %}\n\n    {% set level = states(''__EC_CONSUMER__'') %}\n\n    {% set cost = states(''__EC_CONSUMER_COST__'') %}\n\n    **{{ {''BOOST'':''🟢 Zwiększ zużycie'',''CHEAP'':''🟢 Tanie zużycie'',''NORMAL'':''⚪ Zwykłe zużycie'',''LIMIT'':''🔴 Ogranicz zużycie''}.get(level,''Brak zalecenia'') }}**\n\n    {% if is_number(cost) %} · {{ cost|float|round(3) }} zł/kWh dodatkowego zużycia{% endif %}\n\n\n    {% set change = states(''__EC_NEXT_CHANGE__'') %}\n\n    {% if as_timestamp(change,0) > as_timestamp(now()) %}\n\n    Następna zmiana: **{{ {''BOOST'':''zwiększ zużycie'',''CHEAP'':''tanie zużycie'',''NORMAL'':''zwykłe zużycie'',''LIMIT'':''ogranicz zużycie''}.get(state_attr(''__EC_NEXT_CHANGE__'',''next_level''),''Brak danych'') }}**, {{ as_local(as_datetime(change)).strftime(''%d.%m %H:%M'') }}.\n\n    {% endif %}\n\n\n    Szacowany koszt dodatkowej energii zużytej w domu, z uwzględnieniem baterii. To nie cena zakupu z sieci.\n\n    {% else %}\n\n    Wskazówka zużycia niedostępna — brak aktualnej prognozy.\n\n    {% endif %}'\n  grid_options:\n    columns: 24\n    rows: auto\n- type: heading\n  heading: Historia\n  heading_style: subtitle\n  icon: mdi:history\n- type: sensor\n  entity: __EC_SOC__\n  name: Naładowanie · ostatnie 24 h\n  graph: line\n  hours_to_show: 24\n  detail: 2\n  grid_options:\n    columns: 12\n    rows: auto\n- type: sensor\n  entity: __EC_BATTERY_POWER__\n  name: Moc · + rozładowanie / − ładowanie\n  graph: line\n  hours_to_show: 24\n  detail: 2\n  grid_options:\n    columns: 12\n    rows: auto\n- type: history-graph\n  title: Historia sterowania · 24 h\n  hours_to_show: 24\n  grid_options:\n    columns: 24\n    rows: auto\n  entities:\n  - entity: input_select.energy_compass_deye_mode\n    name: Sterowanie\n  - entity: __EC_MACHINE__\n    name: Zalecenie baterii\n  - entity: sensor.energy_compass_deye_runtime\n    name: Stan sterownika\n  - entity: __EC_BATTERY_STATE__\n    name: Praca baterii\n",
    "fields": [
     "plan",
     "valid",
     "consumer",
     "soc",
     "alert",
     "machine",
     "consumer_cost",
     "next_change",
     "battery_power",
     "battery_state",
     "capacity"
    ]
   }
  },
  "diagnostics": {
   "en": {
    "yaml": "type: grid\ncards:\n- type: heading\n  heading: Energy Compass diagnostics\n  heading_style: title\n  icon: mdi:wrench-outline\n- type: markdown\n  title: Energy Compass diagnostics\n  content: '{% set labels = {''CHARGE_PV'':''Charging from PV'',''CHARGE_GRID'':''Charging from grid'',''DISCHARGE_GRID'':''Selling to grid'',''SELF_CONSUME'':''Powering the house'',''HOLD'':''Holding'',''CURTAIL'':''Holding (PV curtailment)'',''BASE'':''Base mode''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    | Parameter | State |\n\n    |:--|:--|\n\n    | Optimizer | {{ states(''__EC_OPTIMIZER__'') }} |\n\n    | Controller | {{ rt.get(''code'',''No data'') }} |\n\n    | Reason | {{ rt.get(''reason'',''—'') }} |\n\n    | Confirmed mode | {{ labels.get(rt.get(''confirmed_mode''),rt.get(''confirmed_mode'',''—'')) }} |\n\n    | Last confirmation | {{ as_local(as_datetime(rt.get(''last_confirmation''))).strftime(''%d.%m %H:%M:%S'') if as_timestamp(rt.get(''last_confirmation''),0) else ''None'' }} |\n\n    | Plan created | {{ as_local(as_datetime(state_attr(plan,''generated_at''))).strftime(''%d.%m %H:%M:%S'') if as_timestamp(state_attr(plan,''generated_at''),0) else ''None'' }} |\n\n    | Controller plan valid until | {{ as_local(as_datetime(rt.get(''original_deadline''))).strftime(''%d.%m %H:%M:%S'') if as_timestamp(rt.get(''original_deadline''),0) else ''None'' }} |\n\n    | Forecast until | {{ as_local(as_datetime(state_attr(''__EC_VALID__'',''coverage_end''))).strftime(''%d.%m %H:%M'') if as_timestamp(state_attr(''__EC_VALID__'',''coverage_end''),0) else ''None'' }} |\n\n    | Active TOU program | {{ rt.get(''active_tou'',''—'') }} |\n\n    | Unconfirmed writes | {{ rt.get(''uncertain'',[])|length }} |\n\n    | Controller session | {{ states(''input_boolean.energy_compass_deye_session'') }} |\n\n    | Restore after the session | {{ states(''input_boolean.energy_compass_deye_restore_pending'') }} |\n\n\n    {% if rt.get(''warning'') %}⚠️ {{ rt.warning }}{% endif %}\n\n\n    {% if state_attr(plan,''reasons'') %}Model notes: {{ state_attr(plan,''reasons'')|join('', '') }}.{% endif %}\n\n\n    The restore flag can be on during an Auto session. On its own it does not mean a fault.'\n- type: entities\n  title: Inverter limits · read-back\n  show_header_toggle: false\n  entities:\n  - type: simple-entity\n    entity: __EC_CHARGE_LIMIT__\n    name: Charge limit\n  - type: simple-entity\n    entity: __EC_DISCHARGE_LIMIT__\n    name: Discharge limit\n  - type: simple-entity\n    entity: __EC_GRID_LIMIT__\n    name: Grid charge limit\n  - type: simple-entity\n    entity: sensor.energy_compass_deye_next_tou\n    name: Next TOU change\n",
    "fields": [
     "plan",
     "valid",
     "optimizer",
     "charge_limit",
     "discharge_limit",
     "grid_limit"
    ]
   },
   "pl": {
    "yaml": "type: grid\ncards:\n- type: heading\n  heading: Diagnostyka Energy Compass\n  heading_style: title\n  icon: mdi:wrench-outline\n- type: markdown\n  title: Diagnostyka Energy Compass\n  content: '{% set labels = {''CHARGE_PV'':''Ładowanie z PV'',''CHARGE_GRID'':''Ładowanie z sieci'',''DISCHARGE_GRID'':''Sprzedaż do sieci'',''SELF_CONSUME'':''Zasilanie domu'',''HOLD'':''Podtrzymanie'',''CURTAIL'':''Podtrzymanie (ograniczenie PV)'',''BASE'':''Tryb bazowy''} %}\n\n    {% set colors = {''CHARGE_PV'':''🟢'',''CHARGE_GRID'':''🔵'',''DISCHARGE_GRID'':''🟠'',''SELF_CONSUME'':''🟣'',''HOLD'':''⚪'',''CURTAIL'':''⚪'',''BASE'':''⚪''} %}\n\n    {% set rt = state_attr(''sensor.energy_compass_deye_runtime'',''runtime'') or {} %}\n\n    {% set plan = ''__EC_PLAN__'' %}\n\n    {% set valid = is_state(''__EC_VALID__'',''on'') and as_timestamp(state_attr(plan,''valid_until''),0) > as_timestamp(now()) %}\n\n    {% set fresh = as_timestamp(rt.get(''original_deadline''),0) > as_timestamp(now()) and as_timestamp(rt.get(''last_confirmation''),0) > 0 %}\n\n    | Parametr | Stan |\n\n    |:--|:--|\n\n    | Optymalizator | {{ states(''__EC_OPTIMIZER__'') }} |\n\n    | Sterownik | {{ rt.get(''code'',''Brak danych'') }} |\n\n    | Powód | {{ rt.get(''reason'',''—'') }} |\n\n    | Tryb potwierdzony | {{ labels.get(rt.get(''confirmed_mode''),rt.get(''confirmed_mode'',''—'')) }} |\n\n    | Ostatnie potwierdzenie | {{ as_local(as_datetime(rt.get(''last_confirmation''))).strftime(''%d.%m %H:%M:%S'') if as_timestamp(rt.get(''last_confirmation''),0) else ''Brak'' }} |\n\n    | Plan utworzony | {{ as_local(as_datetime(state_attr(plan,''generated_at''))).strftime(''%d.%m %H:%M:%S'') if as_timestamp(state_attr(plan,''generated_at''),0) else ''Brak'' }} |\n\n    | Plan sterownika ważny do | {{ as_local(as_datetime(rt.get(''original_deadline''))).strftime(''%d.%m %H:%M:%S'') if as_timestamp(rt.get(''original_deadline''),0) else ''Brak'' }} |\n\n    | Prognoza do | {{ as_local(as_datetime(state_attr(''__EC_VALID__'',''coverage_end''))).strftime(''%d.%m %H:%M'') if as_timestamp(state_attr(''__EC_VALID__'',''coverage_end''),0) else ''Brak'' }} |\n\n    | Aktywny program TOU | {{ rt.get(''active_tou'',''—'') }} |\n\n    | Niepotwierdzone zapisy | {{ rt.get(''uncertain'',[])|length }} |\n\n    | Sesja sterownika | {{ states(''input_boolean.energy_compass_deye_session'') }} |\n\n    | Przywrócenie po zakończeniu sesji | {{ states(''input_boolean.energy_compass_deye_restore_pending'') }} |\n\n\n    {% if rt.get(''warning'') %}⚠️ {{ rt.warning }}{% endif %}\n\n\n    {% if state_attr(plan,''reasons'') %}Uwagi modelu: {{ state_attr(plan,''reasons'')|join('', '') }}.{% endif %}\n\n\n    Flaga przywrócenia może być aktywna podczas sesji Auto. Sama nie oznacza awarii.'\n- type: entities\n  title: Limity falownika · odczyt\n  show_header_toggle: false\n  entities:\n  - type: simple-entity\n    entity: __EC_CHARGE_LIMIT__\n    name: Limit ładowania\n  - type: simple-entity\n    entity: __EC_DISCHARGE_LIMIT__\n    name: Limit rozładowania\n  - type: simple-entity\n    entity: __EC_GRID_LIMIT__\n    name: Limit ładowania z sieci\n  - type: simple-entity\n    entity: sensor.energy_compass_deye_next_tou\n    name: Następna zmiana TOU\n",
    "fields": [
     "plan",
     "valid",
     "optimizer",
     "charge_limit",
     "discharge_limit",
     "grid_limit"
    ]
   }
  },
  "cost": {
   "en": {
    "yaml": "type: grid\ncolumn_span: 4\ncards:\n- type: custom:energy-compass-cost-card\n  language: en\n  currency: PLN\n  cost_entity: __EC_IMPORT_COST__\n  import_entity: __EC_IMPORT_ENERGY__\n  export_entity: __EC_EXPORT_ENERGY__\n  import_price_entity: __EC_IMPORT_PRICE__\n  export_prices_entity: __EC_EXPORT_PRICES__\n  deposit_entity: __EC_DEPOSIT__\n  plan_entity: __EC_PLAN__\n  valid_entity: __EC_VALID__\n",
    "fields": [
     "plan",
     "valid",
     "import_cost",
     "import_energy",
     "export_energy",
     "import_price",
     "export_prices",
     "deposit"
    ]
   },
   "pl": {
    "yaml": "type: grid\ncolumn_span: 4\ncards:\n- type: custom:energy-compass-cost-card\n  language: pl\n  currency: PLN\n  cost_entity: __EC_IMPORT_COST__\n  import_entity: __EC_IMPORT_ENERGY__\n  export_entity: __EC_EXPORT_ENERGY__\n  import_price_entity: __EC_IMPORT_PRICE__\n  export_prices_entity: __EC_EXPORT_PRICES__\n  deposit_entity: __EC_DEPOSIT__\n  plan_entity: __EC_PLAN__\n  valid_entity: __EC_VALID__\n",
    "fields": [
     "plan",
     "valid",
     "import_cost",
     "import_energy",
     "export_energy",
     "import_price",
     "export_prices",
     "deposit"
    ]
   }
  },
  "package": {
   "en": {
    "yaml": "input_select:\n  energy_compass_deye_mode:\n    name: Energy Compass Deye mode\n    options:\n    - 'Off'\n    - Simulation\n    - Auto\n    icon: mdi:compass\ninput_boolean:\n  energy_compass_deye_session:\n    name: Energy Compass Deye session ready\n    initial: false\n  energy_compass_deye_restore_pending:\n    name: Energy Compass Deye restore pending\ninput_datetime:\n  energy_compass_deye_session_start:\n    name: Energy Compass Deye session start\n    has_date: true\n    has_time: true\ntemplate:\n- triggers:\n  - trigger: event\n    event_type: energy_compass_deye_accept_plan\n  sensor:\n  - name: Energy Compass Deye plan\n    unique_id: energy_compass_deye_plan\n    state: '{{ trigger.event.data.snapshot.generated_at }}'\n    attributes:\n      snapshot: '{{ trigger.event.data.snapshot }}'\n- triggers:\n  - trigger: event\n    event_type: energy_compass_deye_runtime\n  sensor:\n  - name: Energy Compass Deye runtime\n    unique_id: energy_compass_deye_runtime\n    state: '{{ trigger.event.data.runtime.get(\"code\", \"waiting\") }}'\n    attributes:\n      runtime: '{{ trigger.event.data.runtime }}'\n- sensor:\n  - name: Energy Compass Deye TOU settings\n    unique_id: energy_compass_deye_tou_settings\n    state: '{% set ns = namespace(v={}) %}{% for i in range(1,7) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_power'', states(''number.__EC_PREFIX__'' ~ i ~ ''_power''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_voltage'', states(''number.__EC_PREFIX__'' ~ i ~ ''_voltage''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_soc'', states(''number.__EC_PREFIX__'' ~ i ~ ''_soc''))])) %}{% set ns.v = dict(ns.v, **dict([(''select.__EC_PREFIX__'' ~ i ~ ''_charging'', states(''select.__EC_PREFIX__'' ~ i ~ ''_charging''))])) %}{% set ns.v = dict(ns.v, **dict([(''time.__EC_PREFIX__'' ~ i ~ ''_time'', states(''time.__EC_PREFIX__'' ~ i ~ ''_time''))])) %}{% endfor %}{{ ns.v.values()|reject(''in'', [''unknown'',''unavailable''])|list|length }}'\n    attributes:\n      prefix: __EC_PREFIX__\n      values: '{% set ns = namespace(v={}) %}{% for i in range(1,7) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_power'', states(''number.__EC_PREFIX__'' ~ i ~ ''_power''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_voltage'', states(''number.__EC_PREFIX__'' ~ i ~ ''_voltage''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_soc'', states(''number.__EC_PREFIX__'' ~ i ~ ''_soc''))])) %}{% set ns.v = dict(ns.v, **dict([(''select.__EC_PREFIX__'' ~ i ~ ''_charging'', states(''select.__EC_PREFIX__'' ~ i ~ ''_charging''))])) %}{% set ns.v = dict(ns.v, **dict([(''time.__EC_PREFIX__'' ~ i ~ ''_time'', states(''time.__EC_PREFIX__'' ~ i ~ ''_time''))])) %}{% endfor %}{{ ns.v }}'\n  - name: Energy Compass Deye next TOU\n    unique_id: energy_compass_deye_next_tou\n    device_class: timestamp\n    state: '{% set ns=namespace(times=[]) %}{% for i in range(1,7) %}{% set raw=states(''time.__EC_PREFIX__''~i~''_time'') %}{% if raw not in [''unknown'',''unavailable''] %}{% set at=today_at(raw) %}{% set at=at if at > now() else at + timedelta(days=1) %}{% set ns.times=ns.times+[at] %}{% endif %}{% endfor %}{{ (ns.times|min).isoformat() if ns.times else none }}'\n  - name: Energy Compass Deye deadline\n    unique_id: energy_compass_deye_deadline\n    device_class: timestamp\n    state: '{{ (state_attr(''sensor.energy_compass_deye_plan'',''snapshot'') or {}).get(''valid_until'') }}'\n  - name: Energy Compass Deye interval end\n    unique_id: energy_compass_deye_interval_end\n    device_class: timestamp\n    state: '{% set c=state_attr(''sensor.energy_compass_deye_plan'',''snapshot'') or {} %}{% set ns=namespace(end=none) %}{% for r in c.get(''intervals'',[]) %}{% if as_timestamp(r.start) <= as_timestamp(now()) < as_timestamp(r.end) %}{% set ns.end=r.end %}{% endif %}{% endfor %}{{ ns.end }}'\n",
    "fields": [
     "prefix"
    ]
   },
   "pl": {
    "yaml": "input_select:\n  energy_compass_deye_mode:\n    name: Energy Compass Deye mode\n    options:\n    - 'Off'\n    - Simulation\n    - Auto\n    icon: mdi:compass\ninput_boolean:\n  energy_compass_deye_session:\n    name: Energy Compass Deye session ready\n    initial: false\n  energy_compass_deye_restore_pending:\n    name: Energy Compass Deye restore pending\ninput_datetime:\n  energy_compass_deye_session_start:\n    name: Energy Compass Deye session start\n    has_date: true\n    has_time: true\ntemplate:\n- triggers:\n  - trigger: event\n    event_type: energy_compass_deye_accept_plan\n  sensor:\n  - name: Energy Compass Deye plan\n    unique_id: energy_compass_deye_plan\n    state: '{{ trigger.event.data.snapshot.generated_at }}'\n    attributes:\n      snapshot: '{{ trigger.event.data.snapshot }}'\n- triggers:\n  - trigger: event\n    event_type: energy_compass_deye_runtime\n  sensor:\n  - name: Energy Compass Deye runtime\n    unique_id: energy_compass_deye_runtime\n    state: '{{ trigger.event.data.runtime.get(\"code\", \"waiting\") }}'\n    attributes:\n      runtime: '{{ trigger.event.data.runtime }}'\n- sensor:\n  - name: Energy Compass Deye TOU settings\n    unique_id: energy_compass_deye_tou_settings\n    state: '{% set ns = namespace(v={}) %}{% for i in range(1,7) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_power'', states(''number.__EC_PREFIX__'' ~ i ~ ''_power''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_voltage'', states(''number.__EC_PREFIX__'' ~ i ~ ''_voltage''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_soc'', states(''number.__EC_PREFIX__'' ~ i ~ ''_soc''))])) %}{% set ns.v = dict(ns.v, **dict([(''select.__EC_PREFIX__'' ~ i ~ ''_charging'', states(''select.__EC_PREFIX__'' ~ i ~ ''_charging''))])) %}{% set ns.v = dict(ns.v, **dict([(''time.__EC_PREFIX__'' ~ i ~ ''_time'', states(''time.__EC_PREFIX__'' ~ i ~ ''_time''))])) %}{% endfor %}{{ ns.v.values()|reject(''in'', [''unknown'',''unavailable''])|list|length }}'\n    attributes:\n      prefix: __EC_PREFIX__\n      values: '{% set ns = namespace(v={}) %}{% for i in range(1,7) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_power'', states(''number.__EC_PREFIX__'' ~ i ~ ''_power''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_voltage'', states(''number.__EC_PREFIX__'' ~ i ~ ''_voltage''))])) %}{% set ns.v = dict(ns.v, **dict([(''number.__EC_PREFIX__'' ~ i ~ ''_soc'', states(''number.__EC_PREFIX__'' ~ i ~ ''_soc''))])) %}{% set ns.v = dict(ns.v, **dict([(''select.__EC_PREFIX__'' ~ i ~ ''_charging'', states(''select.__EC_PREFIX__'' ~ i ~ ''_charging''))])) %}{% set ns.v = dict(ns.v, **dict([(''time.__EC_PREFIX__'' ~ i ~ ''_time'', states(''time.__EC_PREFIX__'' ~ i ~ ''_time''))])) %}{% endfor %}{{ ns.v }}'\n  - name: Energy Compass Deye next TOU\n    unique_id: energy_compass_deye_next_tou\n    device_class: timestamp\n    state: '{% set ns=namespace(times=[]) %}{% for i in range(1,7) %}{% set raw=states(''time.__EC_PREFIX__''~i~''_time'') %}{% if raw not in [''unknown'',''unavailable''] %}{% set at=today_at(raw) %}{% set at=at if at > now() else at + timedelta(days=1) %}{% set ns.times=ns.times+[at] %}{% endif %}{% endfor %}{{ (ns.times|min).isoformat() if ns.times else none }}'\n  - name: Energy Compass Deye deadline\n    unique_id: energy_compass_deye_deadline\n    device_class: timestamp\n    state: '{{ (state_attr(''sensor.energy_compass_deye_plan'',''snapshot'') or {}).get(''valid_until'') }}'\n  - name: Energy Compass Deye interval end\n    unique_id: energy_compass_deye_interval_end\n    device_class: timestamp\n    state: '{% set c=state_attr(''sensor.energy_compass_deye_plan'',''snapshot'') or {} %}{% set ns=namespace(end=none) %}{% for r in c.get(''intervals'',[]) %}{% if as_timestamp(r.start) <= as_timestamp(now()) < as_timestamp(r.end) %}{% set ns.end=r.end %}{% endif %}{% endfor %}{{ ns.end }}'\n",
    "fields": [
     "prefix"
    ]
   }
  },
  "tariff": {
   "en": {
    "yaml": "template:\n  - triggers:\n      - trigger: homeassistant\n        event: start\n      - trigger: time_pattern\n        minutes: \"/15\"\n    variables:\n      tariff_group: __EC_TARIFF_GROUP__\n      # Illustrative final PLN/kWh rates; replace these with your own contract prices.\n      base_rate: __EC_BASE_RATE__\n      off_peak_rate: __EC_OFF_PEAK_RATE__\n      # G12/G12w afternoon off-peak window: fixed = 13:00-15:00 all year;\n      # seasonal = 15:00-17:00 April-September, 13:00-15:00 October-March.\n      afternoon_window: __EC_AFTERNOON_WINDOW__\n      # Only dates explicitly listed here receive the G12w off-peak rate.\n      off_peak_dates: []\n      forecast_rows: >-\n        {% if tariff_group is defined and tariff_group in ['G11', 'G12', 'G12w']\n              and base_rate is defined and is_number(base_rate)\n              and off_peak_rate is defined and is_number(off_peak_rate)\n              and afternoon_window is defined and afternoon_window in ['fixed', 'seasonal']\n              and off_peak_dates is defined\n              and off_peak_dates is sequence and off_peak_dates is not string %}\n          {% set rows = namespace(value=[]) %}\n          {% set first_hour = ((as_timestamp(utcnow()) // 3600) * 3600) | int %}\n          {% for hour in range(49) %}\n            {% set start = as_datetime((first_hour + hour * 3600) | timestamp_utc) %}\n            {% set end = as_datetime((first_hour + (hour + 1) * 3600) | timestamp_utc) %}\n            {% set local = as_local(start) %}\n            {% set afternoon = 15 if afternoon_window == 'seasonal' and 4 <= local.month <= 9 else 13 %}\n            {% set off_peak = tariff_group != 'G11' and\n                (local.hour >= 22 or local.hour < 6 or afternoon <= local.hour < afternoon + 2 or\n                (tariff_group == 'G12w' and\n                (local.weekday() >= 5 or local.date().isoformat() in off_peak_dates))) %}\n            {% set rows.value = rows.value + [{\n                'start': start.isoformat(),\n                'end': end.isoformat(),\n                'price': (off_peak_rate if off_peak else base_rate) | float\n            }] %}\n          {% endfor %}\n          {{ rows.value }}\n        {% else %}\n          {{ [] }}\n        {% endif %}\n    sensor:\n      - name: Energy Compass tariff price\n        unique_id: energy_compass_tariff_price_example\n        default_entity_id: sensor.energy_compass_tariff_price\n        unit_of_measurement: PLN/kWh\n        availability: >-\n          {{ tariff_group is defined and tariff_group in ['G11', 'G12', 'G12w']\n             and base_rate is defined and is_number(base_rate)\n             and off_peak_rate is defined and is_number(off_peak_rate)\n             and afternoon_window is defined and afternoon_window in ['fixed', 'seasonal']\n             and off_peak_dates is defined\n             and off_peak_dates is sequence and off_peak_dates is not string }}\n        state: \"{{ forecast_rows[0].price if forecast_rows | count == 49 else none }}\"\n        attributes:\n          forecast: \"{{ forecast_rows }}\"\n          generated_at: \"{{ utcnow().isoformat() }}\"\n",
    "fields": [
     "tariff_group",
     "base_rate",
     "off_peak_rate",
     "afternoon_window"
    ]
   },
   "pl": {
    "yaml": "template:\n  - triggers:\n      - trigger: homeassistant\n        event: start\n      - trigger: time_pattern\n        minutes: \"/15\"\n    variables:\n      tariff_group: __EC_TARIFF_GROUP__\n      # Illustrative final PLN/kWh rates; replace these with your own contract prices.\n      base_rate: __EC_BASE_RATE__\n      off_peak_rate: __EC_OFF_PEAK_RATE__\n      # G12/G12w afternoon off-peak window: fixed = 13:00-15:00 all year;\n      # seasonal = 15:00-17:00 April-September, 13:00-15:00 October-March.\n      afternoon_window: __EC_AFTERNOON_WINDOW__\n      # Only dates explicitly listed here receive the G12w off-peak rate.\n      off_peak_dates: []\n      forecast_rows: >-\n        {% if tariff_group is defined and tariff_group in ['G11', 'G12', 'G12w']\n              and base_rate is defined and is_number(base_rate)\n              and off_peak_rate is defined and is_number(off_peak_rate)\n              and afternoon_window is defined and afternoon_window in ['fixed', 'seasonal']\n              and off_peak_dates is defined\n              and off_peak_dates is sequence and off_peak_dates is not string %}\n          {% set rows = namespace(value=[]) %}\n          {% set first_hour = ((as_timestamp(utcnow()) // 3600) * 3600) | int %}\n          {% for hour in range(49) %}\n            {% set start = as_datetime((first_hour + hour * 3600) | timestamp_utc) %}\n            {% set end = as_datetime((first_hour + (hour + 1) * 3600) | timestamp_utc) %}\n            {% set local = as_local(start) %}\n            {% set afternoon = 15 if afternoon_window == 'seasonal' and 4 <= local.month <= 9 else 13 %}\n            {% set off_peak = tariff_group != 'G11' and\n                (local.hour >= 22 or local.hour < 6 or afternoon <= local.hour < afternoon + 2 or\n                (tariff_group == 'G12w' and\n                (local.weekday() >= 5 or local.date().isoformat() in off_peak_dates))) %}\n            {% set rows.value = rows.value + [{\n                'start': start.isoformat(),\n                'end': end.isoformat(),\n                'price': (off_peak_rate if off_peak else base_rate) | float\n            }] %}\n          {% endfor %}\n          {{ rows.value }}\n        {% else %}\n          {{ [] }}\n        {% endif %}\n    sensor:\n      - name: Energy Compass tariff price\n        unique_id: energy_compass_tariff_price_example\n        default_entity_id: sensor.energy_compass_tariff_price\n        unit_of_measurement: PLN/kWh\n        availability: >-\n          {{ tariff_group is defined and tariff_group in ['G11', 'G12', 'G12w']\n             and base_rate is defined and is_number(base_rate)\n             and off_peak_rate is defined and is_number(off_peak_rate)\n             and afternoon_window is defined and afternoon_window in ['fixed', 'seasonal']\n             and off_peak_dates is defined\n             and off_peak_dates is sequence and off_peak_dates is not string }}\n        state: \"{{ forecast_rows[0].price if forecast_rows | count == 49 else none }}\"\n        attributes:\n          forecast: \"{{ forecast_rows }}\"\n          generated_at: \"{{ utcnow().isoformat() }}\"\n",
    "fields": [
     "tariff_group",
     "base_rate",
     "off_peak_rate",
     "afternoon_window"
    ]
   }
  },
  "rce": {
   "en": {
    "yaml": "# Polish net-billing sell price from RCE (PSE market price), as an Energy Compass\n# interval forecast. See docs/tariff-helper.md § RCE sell price.\n#\n# The REST sensor fetches today's and tomorrow's quarter-hour RCE prices\n# (PLN/MWh) from the public PSE API every 30 minutes. The Template sensor turns\n# them into PLN/kWh with a floor and a multiplier (net-billing deposit: negative\n# prices count as zero, then x 1.23) and publishes them as `prices` records.\n# Timestamps use the compact UTC form 20260925T1015Z to stay under Home\n# Assistant's recorder attribute size limit.\nsensor:\n  - platform: rest\n    name: Energy Compass RCE Raw\n    unique_id: energy_compass_rce_raw\n    resource: https://api.raporty.pse.pl/api/rce-pln\n    params:\n      \"$select\": dtime_utc,rce_pln,business_date\n      \"$filter\": >-\n        business_date ge '{{ now().date() }}' and business_date le '{{ (now() + timedelta(days=1)).date() }}'\n      \"$first\": \"200\"\n    headers:\n      Accept: application/json\n    scan_interval: 1800\n    timeout: 30\n    availability: >-\n      {{ value_json is mapping\n         and value_json.get('value') is sequence\n         and value_json.get('value') is not string\n         and 0 < value_json.get('value') | count <= 200\n         and not value_json.get('@nextLink')\n         and not value_json.get('@odata.nextLink')\n         and not value_json.get('nextLink') }}\n    value_template: \"{{ utcnow().isoformat() }}\"\n    json_attributes:\n      - value\n\ntemplate:\n  - triggers:\n      - trigger: homeassistant\n        event: start\n      - trigger: time_pattern\n        minutes: \"/15\"\n      - trigger: state\n        entity_id: sensor.energy_compass_rce_raw\n    variables:\n      # Final sell price = max(RCE, floor) x multiplier / 1000 (PLN/MWh -> PLN/kWh).\n      # Net-billing deposit: floor 0, multiplier 1.23. Adjust to your contract.\n      multiplier: __EC_MULTIPLIER__\n      floor: __EC_FLOOR__\n      source: sensor.energy_compass_rce_raw\n      fetched: \"{{ as_timestamp(states(source), none) }}\"\n      sell_rows: >-\n        {% set raw = state_attr(source, 'value') %}\n        {% set ns = namespace(rows=[], valid=is_number(multiplier) and is_number(floor)\n                              and raw is sequence and raw is not string and raw | count > 0) %}\n        {% for row in (raw if ns.valid else []) %}\n          {% if row is not mapping or row.get('dtime_utc') is not string\n                or row.get('rce_pln') is boolean or not is_number(row.get('rce_pln')) %}\n            {% set ns.valid = false %}\n          {% else %}\n            {% set end = as_datetime(row.dtime_utc ~ '+00:00', none) %}\n            {% if end is none %}\n              {% set ns.valid = false %}\n            {% else %}\n              {% set price = ([row.rce_pln | float, floor | float] | max) * multiplier | float / 1000 %}\n              {% set ns.rows = ns.rows + [dict(start=(end - timedelta(minutes=15)).strftime('%Y%m%dT%H%MZ'), end=end.strftime('%Y%m%dT%H%MZ'), price=price)] %}\n            {% endif %}\n          {% endif %}\n        {% endfor %}\n        {{ (ns.rows | sort(attribute='start')) if ns.valid else [] }}\n      current_price: >-\n        {% set now_key = utcnow().strftime('%Y%m%dT%H%MZ') %}\n        {% set ns = namespace(price=none) %}\n        {% for row in sell_rows if row.start <= now_key < row.end %}\n          {% set ns.price = row.price %}\n        {% endfor %}\n        {{ ns.price }}\n    sensor:\n      - name: Energy Compass RCE export forecast\n        unique_id: energy_compass_rce_export_forecast\n        default_entity_id: sensor.energy_compass_rce_export_forecast\n        unit_of_measurement: PLN/kWh\n        availability: >-\n          {{ fetched is not none and 0 <= as_timestamp(now()) - fetched <= 4500\n             and is_number(current_price) }}\n        state: \"{{ current_price }}\"\n        attributes:\n          prices: \"{{ sell_rows }}\"\n          published_at: \"{{ states(source) }}\"\n          settlement: \"RCE, floor {{ floor }}, multiplier {{ multiplier }}\"\n",
    "fields": [
     "multiplier",
     "floor"
    ]
   },
   "pl": {
    "yaml": "# Polish net-billing sell price from RCE (PSE market price), as an Energy Compass\n# interval forecast. See docs/tariff-helper.md § RCE sell price.\n#\n# The REST sensor fetches today's and tomorrow's quarter-hour RCE prices\n# (PLN/MWh) from the public PSE API every 30 minutes. The Template sensor turns\n# them into PLN/kWh with a floor and a multiplier (net-billing deposit: negative\n# prices count as zero, then x 1.23) and publishes them as `prices` records.\n# Timestamps use the compact UTC form 20260925T1015Z to stay under Home\n# Assistant's recorder attribute size limit.\nsensor:\n  - platform: rest\n    name: Energy Compass RCE Raw\n    unique_id: energy_compass_rce_raw\n    resource: https://api.raporty.pse.pl/api/rce-pln\n    params:\n      \"$select\": dtime_utc,rce_pln,business_date\n      \"$filter\": >-\n        business_date ge '{{ now().date() }}' and business_date le '{{ (now() + timedelta(days=1)).date() }}'\n      \"$first\": \"200\"\n    headers:\n      Accept: application/json\n    scan_interval: 1800\n    timeout: 30\n    availability: >-\n      {{ value_json is mapping\n         and value_json.get('value') is sequence\n         and value_json.get('value') is not string\n         and 0 < value_json.get('value') | count <= 200\n         and not value_json.get('@nextLink')\n         and not value_json.get('@odata.nextLink')\n         and not value_json.get('nextLink') }}\n    value_template: \"{{ utcnow().isoformat() }}\"\n    json_attributes:\n      - value\n\ntemplate:\n  - triggers:\n      - trigger: homeassistant\n        event: start\n      - trigger: time_pattern\n        minutes: \"/15\"\n      - trigger: state\n        entity_id: sensor.energy_compass_rce_raw\n    variables:\n      # Final sell price = max(RCE, floor) x multiplier / 1000 (PLN/MWh -> PLN/kWh).\n      # Net-billing deposit: floor 0, multiplier 1.23. Adjust to your contract.\n      multiplier: __EC_MULTIPLIER__\n      floor: __EC_FLOOR__\n      source: sensor.energy_compass_rce_raw\n      fetched: \"{{ as_timestamp(states(source), none) }}\"\n      sell_rows: >-\n        {% set raw = state_attr(source, 'value') %}\n        {% set ns = namespace(rows=[], valid=is_number(multiplier) and is_number(floor)\n                              and raw is sequence and raw is not string and raw | count > 0) %}\n        {% for row in (raw if ns.valid else []) %}\n          {% if row is not mapping or row.get('dtime_utc') is not string\n                or row.get('rce_pln') is boolean or not is_number(row.get('rce_pln')) %}\n            {% set ns.valid = false %}\n          {% else %}\n            {% set end = as_datetime(row.dtime_utc ~ '+00:00', none) %}\n            {% if end is none %}\n              {% set ns.valid = false %}\n            {% else %}\n              {% set price = ([row.rce_pln | float, floor | float] | max) * multiplier | float / 1000 %}\n              {% set ns.rows = ns.rows + [dict(start=(end - timedelta(minutes=15)).strftime('%Y%m%dT%H%MZ'), end=end.strftime('%Y%m%dT%H%MZ'), price=price)] %}\n            {% endif %}\n          {% endif %}\n        {% endfor %}\n        {{ (ns.rows | sort(attribute='start')) if ns.valid else [] }}\n      current_price: >-\n        {% set now_key = utcnow().strftime('%Y%m%dT%H%MZ') %}\n        {% set ns = namespace(price=none) %}\n        {% for row in sell_rows if row.start <= now_key < row.end %}\n          {% set ns.price = row.price %}\n        {% endfor %}\n        {{ ns.price }}\n    sensor:\n      - name: Energy Compass RCE export forecast\n        unique_id: energy_compass_rce_export_forecast\n        default_entity_id: sensor.energy_compass_rce_export_forecast\n        unit_of_measurement: PLN/kWh\n        availability: >-\n          {{ fetched is not none and 0 <= as_timestamp(now()) - fetched <= 4500\n             and is_number(current_price) }}\n        state: \"{{ current_price }}\"\n        attributes:\n          prices: \"{{ sell_rows }}\"\n          published_at: \"{{ states(source) }}\"\n          settlement: \"RCE, floor {{ floor }}, multiplier {{ multiplier }}\"\n",
    "fields": [
     "multiplier",
     "floor"
    ]
   }
  }
 }
};
